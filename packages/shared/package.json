{
  "name": "@gameskeep/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "GamesKeep shared domain types + validation (single source of truth for backend and, later, frontend).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./constants": "./src/constants.ts",
    "./validation": "./src/validation.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
