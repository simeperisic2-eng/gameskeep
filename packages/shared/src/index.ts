/**
 * @gameskeep/shared — domain types + input validation shared between the
 * backend (now) and the frontend (later phases). Keep this package free of
 * runtime/server dependencies so any workspace can import it.
 */
export * from './constants';
export * from './validation';
