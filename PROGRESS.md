# GamesKeep — Progress Log

> The agent appends a short entry at the end of each phase/session so the next session knows where to continue.
> Keep entries short: what was done, what's verified, what's next. Newest at the top.

---

## Template (copy for each entry)
```
### [Phase Ix] — YYYY-MM-DD
- Done: <1–3 lines, what was built>
- Verified: <tests pass / build clean / etc.>
- Next: <what the next session should pick up>
- Notes/decisions: <any choice or tradeoff made along the way, if relevant>
```

---

## Log

### [Rebuild Phase 9 — I8 Control Panel · Slice 3: Newsletter (compose / segment / send / GDPR)] — 2026-08-20
- Done: **Slice 3** — newsletter campaigns over a **swappable send seam** (Mock → `email_outbox`, ZERO network; Live dormant) with **no new AI**. Model (migration **0013**): **`newsletter_campaigns`** (subject/preheader/body, `segment`, `kind` manual|digest, `status` draft→scheduled→sending→sent|canceled, schedule, aggregate recipient/opens/clicks counters, createdBy). Reuses the I7 `newsletter_subscriptions` + the I6 `EmailSender` seam — nothing rebuilt. **Send** fans out one plain-text email per recipient through the **Mock sender → outbox** (each carries that recipient's own unsubscribe link), then records the aggregate recipient count; audited. **AI-digest** = `buildDigest()` assembles a draft from the EXISTING `topics.tldr`/`ai_summary` (top recent summarized stories) — generates nothing. **Segmentation is GDPR-gated at SEND time** (`resolveAudience`): active + in-segment + **not-withdrawn** (a registered subscriber whose latest marketing consent is a withdrawal is excluded via the `user_consents` ledger, independent of the `active` flag). **Control Panel → Newsletter** (`/admin/newsletter`, admin-40): subscriber stats, 8-week real growth sparkline, compose form (+ "Generate digest draft"), campaigns table with per-row Send, subscriber list (search / unsubscribe / CSV export at a dot-free `/newsletter/subscribers/export`). Public **login-free `/unsubscribe`** page (capability-token, no session/CSRF, no auto-unsub on prefetch) + its BFF + backend `POST /public/newsletter/unsubscribe`.
- Fixes (as part of the slice): (1) **GDPR consent-sync gap** — `POST /auth/consent {marketing, granted:false}` withdrew consent in the ledger but left the subscription `active=true`; fixed belt-and-braces (the send-time resolver excludes withdrawers independently, AND `/auth/consent` withdrawal now also deactivates the subscription via `deactivateSubscriptionForUser`). (2) **Real bug caught by verify** — the digest's auto-generated `preheader` (3 concatenated topic titles) overflowed `varchar(200)` and crashed the send with a DB error; fixed by clamping subject/preheader to the column limit (anti-bug rule).
- Verified: **`verify:i8` = 21/21** (added 16–21: newsletter section RBAC-gated — moderator 403 / admin 200; marketing-consent withdrawal deactivates the subscription; **Mock send grows the outbox by EXACTLY recipientCount, every new row provider=mock** — zero network; **segmentation excludes a withdrawn user AND a token-unsubscribed address** — both get zero mail; subscriber list is **address-only PII** — no ip/passwordHash/username/displayName; **digest reuses existing summaries** — kind=digest + body carries a real topic slug + its stored summary). **Full from-empty ladder green** (`down -v` → `up --build`): health + `verify:i1…b2` + `verify:i6` + `verify:i7` 30/30 + `verify:i8` 21/21; migration 0013 applies from empty. Static gate: tsc 0, ESLint + Prettier clean, backend vitest 104/104, frontend vitest 7/7, `next build` clean (new routes `/admin/newsletter`, `/unsubscribe`, `/api/newsletter/unsubscribe`). **Visual gate:** the populated Newsletter Control Panel (stats + amber growth sparkline + compose + campaigns SENT/DRAFT + subscribers) desktop + mobile (nav→top, cards stack, tables scroll in their wrappers — no CSS-collapse) + the public unsubscribe page (`scratchpad/i8-shots/newsletter-*`). Owner reviewed + approved the slice.
- Next: **Slice 4 — list/slot configuration** (admin arranges lists/rankings/slots, pins paid/featured games, auto + manual override, all from `app_settings`, nothing hardcoded), then **Slice 5 — account-panel polish** (cosmetic, visual gate only) — both autonomous, committing each. STOP at **Slice 6** (phase close: adversarial security review over the whole I8 diff + strict nonce-CSP deferred from I6; report findings before closing).
- Notes/decisions: consent is enforced at **send time**, not just segment build — a withdrawn address is excluded immediately regardless of when the segment was defined (owner-endorsed). Mock send is safe inline (one INSERT/recipient, no network); Live would move dispatch off the request path (OWNER-TODO). Growth is real (subscription timeline); opens/clicks are structural 0 in demo (no tracking pixel — privacy-first). Digest body is plain text sent text/plain (no HTML/UGC injection surface). The unsubscribe endpoint is CSRF-exempt BY DESIGN (the capability token IS the authorization; the recipient has no session), `no-store`, enumeration-safe.

### [Rebuild Phase 9 — I8 Control Panel · Slice 2: Ad / promotion management (no payment gateway)] — 2026-08-19
- Done: **Slice 2** — inventory + placement lifecycle for on-site advertising, with **no payment infrastructure of any kind** (owner mid-slice decision: dropped the Stripe/payment seam entirely — billing is off-site, an admin sets a placement `active` after payment lands; self-serve Stripe PARKED in OWNER-TODO). Model (migration **0012**): **`ad_slots`** (every on-site slot is now a configurable, admin-editable row — 9 seeded across home/game/games/catalog/sources/source-page/upcoming/awards/awards-year, `fallback` ad|organic|hide, `isActive`) + **`ad_placements`** (advertiser name/contact, **UGC** creative headline/body/ctaUrl/ctaLabel, optional `promotedSubjectId` → a promoted game, `status` draft|scheduled|active|ended admin-set, optional window, aggregate impressions/clicks counters, price for records only). **Public render is transparency-forced**: `AdSlot` is slot-aware — a live placement (status=active ∧ within window) renders its creative with a **mandatory Promoted flag as prominent as a bias flag** (the label is emitted by the renderer, NOT a placement field — an admin can't ship an unlabeled paid placement), creative **escaped** (React children/attrs, never `dangerouslySetInnerHTML`), CTA `rel="nofollow sponsored noopener"` http(s)-only; unsold → the "AD" fallback. Game-page **Promoted badge** for an active game promotion. **Control Panel → Advertising** (`/admin/ads`, admin-40 gated): inventory across every slot (active/scheduled/free + free-slot count), per-slot **aggregate** analytics (impressions/clicks/CTR/occupancy — no per-user data), placements table with the **manual activation** control (audited). Public **"Promote your game"** page (`/promote`) → email enquiry (mailto, no backend form) + footer/sitemap links. Backend `ads/service.ts` (leak-proof public views expose creative only, never price/contact/notes) + `admin/ad-routes.ts` + public `/public/adslot/:key` + `/public/promotion/:slug`.
- Verified: **`verify:i8` = 15/15** (added 11–15: ads section RBAC-gated — moderator 403 / admin 200; **placement creative stored raw, rendered ESCAPED** on the public page — a `<script>` headline can't break out; **activation is admins-only**; an active placement **always carries the Promoted label** even when the creative omits it; the public ad-slot payload is **leak-proof** — creative only; the promoted-game badge resolves + the game page shows Promoted). **Full from-empty ladder green** (`down -v` → `up --build`): health + `verify:i1…b2` + `verify:i6` + `verify:i7` 30/30 + `verify:i8` 15/15; migration 0012 applies from empty; 9 slots seeded. Static gate: tsc 0 (backend+frontend+shared), ESLint + Prettier clean, backend vitest 104/104, frontend vitest 7/7, `next build` clean. **Visual gate:** the public promoted renders — home promoted slot (amber "PROMOTED" flag + creative + "Paid promotion · advertiser" + CTA), the game-page Promoted header badge + sidebar slot, the Promote page — desktop + mobile, no CSS-collapse (`scratchpad/i8-shots/`). The `/admin/ads` screenshot hit a headless-CDP session-cookie quirk (page proven functional via curl → 200 rendering Inventory/Analytics/Placements; the Slice-1 admin dashboard shots confirm the panel chrome). Owner reviewed + approved the slice.
- Next: **Slice 3 — Newsletter** (a GATED slice): compose/schedule/segment, AI-digest reusing the existing summary output (no new AI), **swappable send seam Mock→outbox (zero-network)**, subscriber management, growth analytics from real subscription data. Prove-the-attack-fails: segmentation excludes non-consented addresses, Mock send writes only to the outbox, no PII beyond the address; plus a GDPR check on segmentation/consent. STOP for owner review before Slice 4.
- Notes/decisions: **no payment code anywhere** (owner: on-site payment is out; the manual draft→scheduled→active→ended lifecycle matches the real off-site-billing flow). The Promoted label being **render-enforced, not a field**, cements the transparency rule as a system guarantee (proven by the verify check that omits it and sees the label anyway). Advertiser creative treated as UGC from the start (stored raw, escaped on render) closes the new injection surface. Analytics are **aggregate-only** (GDPR-consistent). Impressions/clicks are mock in the demo (no per-render write path).

### [Rebuild Phase 9 — I8 Control Panel · Slice 1: Control Panel shell + Dashboard (session RBAC)] — 2026-08-19
- Done: Wrote **SPEC-I8-control-panel.md** (owner-approved: UI-over-RBAC, dormant payment/send seams, Promoted-label-always, no-new-AI, single-opt-in, all-configurable, aggregate-only analytics; demo-staff-login decision confirmed). **Slice 1** — the unified, permissioned Control Panel. **The delta this closes:** the admin frontend BFF previously injected the `x-admin-token` server-side, giving the browser console **blanket owner access with no login/RBAC** (the acknowledged I1 stopgap). Now: the Control Panel is behind **staff-session auth** (a non-staff visitor is redirected to login), the admin BFF + SSR reads forward the **session cookie** (RBAC per rank; token retired to automation/verify only), admin mutations double-submit **CSRF** (new shared `adminFetch`), and the nav is **RBAC-filtered** (moderator 30 < admin 40 < owner 50 — a moderator sees only Overview + Moderation; owner also sees Identity). Seeded **demo staff logins** (`demo-moderator`/`demo-admin`/`demo-owner`, documented demo password, `[[OWNER-TODO]]` to remove). New **Dashboard** (`/admin`): aggregate/anonymous overview — counts, 7-day community activity, top topics, source performance, pipeline health; page-view traffic deliberately not tracked (privacy-first). Backend: `/admin/api/dashboard` (section gated at moderator), `/auth/me` now carries `role.rank`.
- Verified: **`verify:i8` = 10/10** — blanket-access fix (anon via the frontend BFF → 401, not a token-proxied 200); anon/non-staff → 401/403; moderator/admin/owner each reach exactly their sections (games/users/roles gated); the BFF forwards the session; session mutations require CSRF (no-header → 403); dashboard is aggregate/anonymous (no email/hash/per-user rows). **Full from-empty ladder green** (health + `verify:i1…b2` + `verify:i6` + `verify:i7` 30/30 + `verify:i8` 10/10). Static gate: tsc 0, ESLint + Prettier clean, backend vitest 104/104, frontend vitest 7/7, `next build` clean. **Visual gate:** dashboard as admin / moderator / owner (RBAC-filtered nav visibly differs) + admin mobile (sidebar → top nav, cards reflow, no CSS-collapse) — `scratchpad/i8-shots/`.
- Next: **Slice 2 — Ad / promotion management** (a GATED slice: screenshot review + new injection surface). Inventory of every slot, sold/free/scheduled, per-slot analytics, "Promote a game" flow, dormant Stripe seam + order model, always-labeled Promoted flag.
- Notes/decisions: the token-proxy retirement is the security spine of I8 — every later admin surface (ads, newsletter) inherits session RBAC + CSRF for free. Demo staff passwords are a documented non-secret (OWNER-TODO to remove). `adminFetch` (raw-Response wrapper) kept the mutation-component diff minimal while adding CSRF everywhere.

### [Rebuild Phase 8 — I7 Awards · Slice 3: public phase-aware UI + archive + winner badge + SEO + security review — I7 CLOSED] — 2026-08-19
- Done: Public **phase-aware `/awards`** (Coming Soon → Voting → Results) + `/awards/[year]` archive + game-page **winner badge** + SEO. Backend public reads (`/awards/current`, `/archive`, `/editions/:year`, `/game/:slug/wins`) assemble a leak-proof, phase-aware view reusing the site's effective-score projection (three separated scores + disconnect per nominee). New `/api/awards` BFF (mirrors the community BFF). Client islands: **`AwardBallot`** (SSR-seeded nominees + live weighted counter + vote), **`AwardsSubscribe`** (explicit-opt-in marketing form). **Critics' vs Community shown SEPARATELY** (score chips + two winner cards, never merged). **Coming-Soon copy** reworked to be honest about timing (owner call): "the best games of {year} · voting opens early {year+1} · winners revealed once voting closes" — the year names the GAMES' year (Game Awards / BAFTA / DICE convention); kept "Awards 2026". The full voting/results/archive UI stays behind the admin phase switch (the demo can show every state).
- Hardening (owner security-review gate): (1) one **`isAwardPublic(edition)`** predicate for BOTH the edition view AND the tally endpoint — closed the classifier/dispatcher visibility leak (a pre-voting/unpublished edition never leaks nominees; the tally 404s); (2) **backward phase moves need `confirm:true`** and are admins-only (moderator 403); (3) strict year parse; (4) subscribe stays single-opt-in with a double-opt-in `[[OWNER-TODO]]` for I8.
- **Adversarial security review** (independent code-read agent + my own live exploit harness; EVERY finding re-verified live before reporting): **M1 (MEDIUM — FIXED)** — the retract (DELETE-vote) path had no window/visibility gate, so it leaked nominees for a non-public edition AND let a voter retract AFTER voting closed (which, since the reveal page recomputes counts live, could flip a recomputed Community Choice post-reveal). Fixed by gating `retractVote` to the OPEN voting window — votes are FROZEN once closed (no new, no removed); cast + retract now share `assertVotingOpen`, no classifier/dispatcher disagreement. Owner-triaged **non-blocking / deferred** (→ OWNER-TODO / I8): generic `award-editions` PATCH bypasses the phase guards; public `weightSum` recon; anonymous-write rate-limit ineffective behind the BFF; subscribe double-opt-in. Residual sock-puppet risk = the accepted community-score posture (email-verification cost + credibility damping now; captcha/device in I8) — stated honestly, not "bulletproof".
- Verified: **`verify:i7` = 30/30** (Slice 3 added 26–30, incl. the M1 regression: retract pre-voting → 409 no-leak; retract after close → 409, vote frozen). **Full from-empty ladder green** (`down -v` → `up --build`): health + `verify:i1…b2` + `verify:i6` + `verify:i7`. Static gate: tsc 0, ESLint + Prettier clean, backend vitest 104/104, frontend vitest 7/7, `next build` clean. **Visual gate:** Coming Soon (new copy) / Voting / Results / Archive / Winner-badge, desktop + mobile — on-brand, no CSS-collapse (`scratchpad/i7-shots/`).
- Next: **I7 is CLOSED.** → **I8** (Control Panel + monetization + newsletter; see BLUEPRINT), which carries the tracked awards follow-ups (double-opt-in, generic-PATCH lifecycle guard, weightSum recon, BFF anonymous-write rate-limit).
- Notes/decisions: the independent review caught M1 — retract + recompute as a COMBINED attack across two legitimate actions — that solo point-probes missed (same value as the I6 independent privesc catch). Discipline held: reported findings, owner triaged, then fixed the bug class (window-gate parity) + added a regression.

### [Rebuild Phase 8 — I7 Awards · Slice 2: subscribe capture (marketing consent) + staff control + analytics] — 2026-08-19
- Done: **Slice 2** (backend + admin API; no UI). **Subscribe capture** — new `newsletter_subscriptions` table (migration **0011**; the site-wide "notify me", awards being the first `source`). Marketing is a SEPARATE GDPR consent from registration, so opt-in is **explicit** (the route rejects a subscribe without `consent:true`), every capture stamps the marketing-consent **version** + a **coarsened ip**, and a **registered** subscribe ALSO writes a canonical `user_consents` kind='marketing' grant (WITHDRAW on unsubscribe) — reusing the existing consent path, **not** a new one (owner directive). An **anonymous** subscriber gets the identical treatment on the row (versioned opt-in + coarsened ip + login-free **unsubscribe token**). Idempotent (one row per email; re-subscribe reactivates). **GDPR wired**: the subscription is included in the data export and **scrubbed on account deletion** (email is PII). **Staff control** (`admin/award-routes.ts`): **guarded phase transition** (`POST /admin/api/awards/editions/:id/phase` — opening `voting` requires published + a window; entering `reveal` **auto-computes** outcomes; backward corrections allowed per AUTO+MANUAL; audited) + an aggregated, **leak-proof analytics** endpoint (voters, votes, subscribers, per-category ratios + outcomes, votes-over-time; geo structurally present but empty — needs a geolocation provider, production-only).
- Verified: **`verify:i7` = 25/25** (added 16–25: consent-required, anonymous same-treatment, registered consent grant, idempotent, token unsubscribe, registered withdrawal, phase guard, reveal auto-compute, leak-proof analytics, delete scrubs the subscription). **Full from-empty ladder green** (`down -v` → `up --build`): health + `verify:i1…b2` + `verify:i6` (GDPR export/delete still pass with the subscription additions) + `verify:i7`; migration 0011 applies from empty. Static gate: tsc 0, ESLint + Prettier clean, backend vitest 104/104. Committed + pushed autonomously (owner authorization).
- Next: **Slice 3** — public phase-aware Awards UI (Coming-Soon default + subscribe form; live counter; Critics vs Community) + archive + game-page winner badge + SEO. Then STOP for the owner's screenshot review **and an adversarial security review over the whole Awards voting flow** before closing I7.
- Notes/decisions: geo analytics is honestly empty in demo (no geolocation API — same data-source reality as player-counts/FPS); structure is ready for production. `MARKETING_CONSENT_VERSION` is a demo default with an `[[OWNER-TODO]]` to set the real string + opt-in copy.

### [Rebuild Phase 8 — I7 Awards · Slice 1: voting + outcomes engine] — 2026-08-19
- Done: The backend Awards engine (SPEC I7). Voting is a community write, gated **identically to I6** (CSRF + `requireVerified` + per-user `allowWrite`) — the anti-abuse primitives are **reused, not rebuilt**. Vote gate = **published AND phase='voting' AND within window** (the "turn it on" switch); one vote per (user, edition-category) via the existing unique index (re-vote upserts). Per-vote **weight = the same 0→1.0 credibility curve** as the community score, computed at cast time and **frozen** in `award_votes.weight`. **Tally** = per-nominee weighted mass + counts, leak-proof. **Outcomes**: Community Choice = weighted winner (upsert, always fresh); **Critics' Choice = auto-suggested** from the top effective critic score, written **insert-if-absent** so a staff confirm/override survives a re-compute (owner decision). Public `/awards` scope (cast/change/retract + live tally) + a staff `compute-outcomes` trigger in the RBAC'd admin scope.
- Verified: **`verify:i7` = 15/15** (all gates + weighting `0.45` + one-per-category + weighted community winner + auto-suggested critics ≠ community + staff-override-survives-recompute + leak-proof). Full from-empty ladder green; static gate clean; backend vitest 104/104. Committed `601b370` + pushed.
- Next: Slice 2 (subscribe + staff control) — see above.
- Notes/decisions: cast-time frozen weight (deterministic, one-vote-one-fixed-weight) chosen over recompute-at-reveal (single source of truth, no retroactive re-weighting). Owner decisions: phase-driven public page defaulting to Coming Soon; Critics' Choice auto-suggested + staff-confirmed.

### [Rebuild Phase 7 — Static pages: About, Methodology, Contact/Join, Privacy, Terms] — 2026-08-18
- Done: Built the **five static pages** that fill the footer links (delta-inspect found all five routes — `/about`, `/methodology`, `/contact`, `/privacy`, `/terms` — were dead **404s**; the footer already pointed at them). New shared shell **`_components/DocPage.tsx`** (breadcrumbs, visible + `BreadcrumbList` JSON-LD from the same items, header, `.gk-prose` reading column) + a **`gk-doc`/`gk-prose` block** in `site.css` (reading column ~68ch, ruled headings, amber-marker lists, explainer cards, note/callout, mailto CTA) — all on the locked charcoal+amber system. **About** ("Transparency, not authority" — the problem → what we do → the principle). **Methodology** (the important one, BLUEPRINT 3.13): frames the *approach* — two bias axes (Influence=flags, Quality=scale), three never-merged rating layers, the disconnect-with-context, credibility-weighted/manipulation-aware community score — and **explicitly withholds** the exact formulas/weights/thresholds ("The approach is public. The dials are not.") with a "why we hold the numbers back" callout. **Contact & join**: `mailto:wrathsystems@gmail.com` (plain + prefilled "Pitch to write"), writer invite, explicit "no sign-up form yet — on purpose" (submission/moderation is a later phase, no backend). **Privacy/GDPR** + **Terms**: full-looking drafts referencing the real GDPR features (export, anonymize-delete, versioned consent), each with a prominent **"Draft — not yet legally reviewed"** notice + `[[OWNER-TODO]]` + `noindex`. Sitemap: added About/Methodology/Contact; **deliberately excluded Privacy/Terms** (noindex drafts — listing a noindex URL contradicts its meta; add once counsel clears them). All pages statically prerendered (○), leak-proof (no data reads), canonical + OG/Twitter + breadcrumbs.
- Verified: Static gate — **tsc 0, ESLint clean, Prettier clean, frontend vitest 7/7**; `next build` prerenders all five as **○ Static**. SEO (live): canonical correct on all five; robots = `index,follow` on About/Methodology/Contact and **`noindex,follow`** on Privacy/Terms; `BreadcrumbList` JSON-LD on every page; sitemap lists the three public routes only. **Visual gate:** CDP screenshots desktop **and** mobile (device-emulated) for all five (`scratchpad/phase7-shots/*`) — no CSS-collapse; cards stack, header/footer collapse, reading column holds. Frontend-only change → rebuilt the frontend container (no backend/DB/schema touched, so the from-empty DB ladder is unaffected). Owner reviewed the screenshots and approved.
- Next: **I7 — Awards** (see BLUEPRINT.md → Implementation Phases).
- Notes/decisions:
  - **Two visual defects found in the screenshot pass and fixed before commit** (owner-endorsed discipline — inspect, don't assume): (1) `.gk-doc-note-label` was `inline-block` so the note heading ran into its body text ("DRAFT — NOT YET LEGALLY REVIEWED**This Privacy…**") → made it `block`; (2) the Contact CTA buttons rendered amber-on-amber (near-invisible) because `.gk-prose a` (specificity 0,1,1) out-ranked `.gk-doc-cta` (0,1,0) and repainted the dark ink → pinned the ink in `.gk-prose .gk-doc-cta`. Both re-screenshotted and confirmed.
  - **noindex on the legal drafts is deliberate** — AI-drafted Privacy/Terms must never be indexed as authoritative before a lawyer reviews them; OWNER-TODO.md carries the ⚖️ legal-review gate (remove the on-page notice + the `noindex` + add both to the sitemap once cleared).
  - Contact email `wrathsystems@gmail.com` used as given, with an OWNER-TODO to confirm the public-facing address before launch.

### [Rebuild Phase 6 — I6 Slice 9: phase close — adversarial security review + fixes + regressions] — 2026-08-16
- Done: Ran the **adversarial security review over the whole I6 diff** (`d91570c..cef30b8`, auth·sessions·UGC·GDPR·RBAC). Read the security core directly and ran live exploits. **Three confirmed vulnerabilities found (reported to the owner, then fixed on approval), plus two lower items:**
  - **🔴 #1 CRITICAL — privilege escalation (admin→owner) via a percent-encoded admin section.** `admin/rbac.ts sectionOf()` classified the section from the RAW `%`-encoded URL (→ admin-40 default) while Fastify decoded the `:resource` param to the real owner-50 resource — a classifier/dispatcher disagreement. PoC: a rank-40 admin `GET /admin/api/%75sers` → 200 (leaked all users), `PATCH /admin/api/%75sers/<self> {roleId: owner}` → rank 40→50. **Fixed belt-and-braces** (owner's call — remove the disagreement, don't patch one encoding): `sectionOf` now DECODES + lowercases before lookup, the guard REJECTS any non-canonical section (403, kills double-encoding), AND a per-resource `minRank` (users/roles = owner-50) is enforced in the handler on the RESOLVED resource, independent of URL-string classification. Service token (owner rank) unaffected.
  - **🟠 #2 HIGH — reflected XSS via JSON-LD** on `/games/browse?genre=`/`?platform=`: the filter echoed into the ItemList `name` and `JSON.stringify` left `<` intact → `</script><script>…` broke out of the `ld+json` block (no CSP). Pre-existing (A1-era) but live; **fixed at the shared emit** — new `lib/jsonld.tsx` `<JsonLd>` escaper (`<`/`>`/`&`/U+2028/U+2029 → `\uXXXX`) wired into all 8 `ld+json` pages, hardening every block at once.
  - **🟡 #3 MEDIUM — open redirect** on `/login?next=`: `router.push(next)` with no same-origin check (PoC landed on `example.com` after login). **Fixed** with `lib/nav.ts safeNext()` (only same-site relative paths; rejects `https://`, `//`, `/\`).
  - **🟢 #4 LOW — error disclosure:** the shared `sendError()` 500 branch echoed `err.message` to public auth/community clients → now a generic body, detail to server logs.
  - **🟢 #5 — missing CSP** (deferred since I5): added a real Content-Security-Policy in `next.config.mjs` (`object-src 'none'`, `base-uri`/`form-action`/`frame-ancestors 'self'`, `connect-src 'self'`; `script/style-src 'unsafe-inline'` since production Next hydrates inline — nonce-strict CSP tracked for I8).
  - **ℹ️ #6 INFO — `csrfOk` made constant-time** (`constantTimeEqual`) for consistency; the duplicate local `csrfOk` in `auth/routes.ts` removed in favor of the shared one.
- Verified: **Each exploit re-run against the rebuilt stack and proven CLOSED** — encoded privesc → 403 (rank stays 40); crafted `?genre=` renders `\uXXXX`-escaped inside `ld+json` (and HTML-entity-escaped in the title); `?next=https://evil.com` lands on `/feed`. **`verify:i6` = 54 checks pass** (added **53** RBAC-privesc-closed: `plain=403 %75sers=403 %72oles=403 dbl=403 escalate=403 rankAfter=40`, and **54** JSON-LD-XSS-closed: `rawBreakout=false escapedInLd=true`). The open-redirect fix is client-only (no server round-trip), so its durable regression is a **frontend unit test on `safeNext()`** (`test/nav.test.ts`, 4 cases) + the browser re-proof — I did not fake an HTTP check for behavior the harness can't observe. **Full from-empty ladder green** (`down -v` → `up --build`): `health` + `verify:i1…b2` (i3 104/105) + `verify:i6` (54). Static gate: tsc 0, ESLint clean, Prettier clean, frontend vitest 7/7 (+3 backend suites). **Visual gate:** home + browse + login (desktop + mobile) render identically under the new CSP — no styling/image/hydration breakage (the redirect re-test also proved client JS runs under CSP).
- Next: **I6 is CLOSED.** Ready for **I7** (see BLUEPRINT.md → Implementation Phases). The nonce-based strict CSP and the field-level admin RBAC/Control-Panel remain tracked for I8.
- Notes/decisions:
  - Discipline that paid off (owner-endorsed): I verified the subagent's XSS claim myself against raw SSR HTML rather than trusting it, ran real end-to-end exploits instead of theorizing, and reported #2 honestly as pre-existing rather than hiding or padding it. The owner directed fixing all four regardless of which slice introduced them (the fix is small and the shared-emit hardening protects every block).
  - The #1 root cause is a bug CLASS — "a classifier and a dispatcher that can disagree." The fix removes the disagreement (decode so the classifier sees what the router sees) rather than blocking one encoding, then backstops it with resolved-resource rank enforcement so access control never depends on URL-string parsing.

### [Rebuild Phase 6 — I6 Slice 8: Public UI (auth pages, profile, account panel, community sections, header menu)] — 2026-08-16
- Done: Built **Slice 8 — Public UI**, the frontend that surfaces every I6 backend slice, delta-first (the auth-BFF + `/community` BFF relays, CSRF double-submit, `force-dynamic` per-user pages, and the client-island-on-cached-page pattern all already existed — reused). **Auth pages** (`/register`, `/login`, `/verify-email`, `/reset-password`): the form shell is **SSR-rendered** so fields paint immediately and hydration only wires submit (fix for the mobile "Loading…" flash) — `AuthForms.tsx` + `lib/client.ts` (fetchMe/apiPost/resetCsrf/MeUser, CSRF-aware). **Public profile** (`/u/[username]`, `public/profile.ts`): level **name** + badges + **join date** + activity counts (ratings/comments), leak-proof — never the raw reputation number/thresholds. **Account/User Panel** (`/account`): level + **progress bar + badges rendered even at zero for a newcomer**, my ratings (shown by **game title, not slug**) / comments / followed, plus `AccountActions.tsx` (data export, delete-account with password re-confirm, sign out). **Header** (`HeaderAuth.tsx` island): signed-out → "Sign in"; signed-in → profile menu (level chip + username → feed/profile/account/sign-out); the header markup stays cacheable, only this island is per-user. **Community sections** filling the I5 "coming with accounts" placeholders: game **rating input + distribution + comments** (`RatingInput.tsx`, `Comments.tsx`), topic **trust/bias votes + discussion** (`BiasVotes.tsx`), upcoming **hype-vote** (`HypeButton.tsx`) — all UGC rendered **escaped** (React auto-escape, no `dangerouslySetInnerHTML`). Nav "Your Feed" → **"Feed"** (was wrapping to two lines on desktop). Cached homepage/public shells unchanged; personalization is all islands + `force-dynamic` per-user pages.
- Verified: **`verify:i6` = 52 attack-proof checks PASS** (Slice 8 added the SSR UGC-escaping check — a stored `<script>` comment renders inert in server HTML). Static gate: **tsc 0 across all workspaces, ESLint clean, Prettier clean**. **Visual gate:** CDP screenshots (`_shots/i6s8-*.png`), **mobile-emulated via `Emulation.setDeviceMetricsOverride({mobile:true})`** (plain `--window-size` does NOT emulate a mobile layout viewport — the source of a false "auth-card overflow" that CDP measurement disproved; card is 358px in a 390px viewport). Login mobile/desktop, register mobile, account mobile/desktop (Newcomer level + progress + Verified badge + ratings as game titles), public profile (Newcomer + "Joined August 2026" + Activity Ratings 3 / Comments 1). **No-regression ladder green from empty** (`docker compose down -v` → `up -d --build`): `health` + `verify:i1…b2` + `verify:i3` (104/105 topic summaries, within the ≤2-missing tolerance) + `verify:i6` (isolated — see note).
- Next: **I6 Slice 9 — phase close**: final `verify:i6`, an **adversarial security review over the whole I6 diff** (auth, sessions, UGC, GDPR, RBAC), the visual gate, and this PROGRESS entry. Report anything the review finds rather than quietly fixing it.
- Notes/decisions:
  - **All 5 owner polish items fixed + 1 self-found issue disclosed:** (1) nav "Feed"; (2) newcomer account panel renders level+progress+badges at zero; (3) ratings show the game title, not the `baldurs-gate-3` slug; (4) SSR auth-form shell (no mobile "Loading…" flash); (5) public profile shows level name + join date + activity counts. **6th, self-found in the visual gate and NOT in the owner list:** the signed-in **mobile** header overflowed (username + level label + burger exceeded 390px) — collapsed the signed-in button to a **compact avatar initial** below 760px (desktop still shows full name + level chip). Disclosed to owner, not shipped silently.
  - **Screenshot-method artifact (root-caused, cost real time):** headless `chrome --window-size=390,844 --screenshot` renders WITHOUT a mobile layout viewport, so responsive layouts appear to overflow when they don't; the fix is CDP device emulation. Also: CDP `Network.setCookie domain=localhost` is stored but not attached to requests (headless quirk) → logged-in shots log in **in-page** via a BFF `fetch` (native Set-Cookie) then navigate; and Windows `kill` doesn't reap `chrome.exe` children so `taskkill //F //IM chrome.exe //T` + a fresh `--remote-debugging-port` between runs is required.
  - **Known harness artifact (not a regression):** running all 14 verify scripts back-to-back momentarily exhausts the shared 100/min rate-limiter budget and trips a rate-sensitive i6 check; **i6 passes cleanly in isolation** (confirmed twice, no ✗). Verify:i3's 3 thin topics (few articles → `summarizeTopic` returns null) accumulate on long-running stacks; a from-empty reset settles at 104/105 (1 thin, within tolerance).

### [Rebuild Phase 6 — I6 Slice 7: GDPR (data export, anonymize-and-tombstone delete, versioned consent)] — 2026-08-15
- Done: Built **Slice 7 — GDPR** (backend; the account UI is Slice 8), delta-first. **The crux, caught in delta-inspection:** 12 tables cascade-delete on user removal, so a hard DELETE would wipe exactly the ratings/votes decision 7 requires keeping — silently shifting every affected game's score on each deletion (and letting someone undo their own extreme vote by deleting the account). So deletion is **anonymize-and-tombstone**: keep the user row, scrub PII, freeze the credibility fields, selectively delete PII children. Migration `0010_i6_gdpr` (`user_consents` table + a `deleted` user status). **Export** (`GET /auth/export`, `private, no-store`) — the authenticated user's OWN data as JSON (profile+email, ratings, trust/bias votes, hype, reactions, comments, follows, consents). **Delete** (`POST /auth/delete-account`) — **re-confirms the password** (a hijacked session alone can't erase), atomic: email→`deleted-*@deleted.invalid`, username freed, password/display/avatar/bio nulled, status→`deleted`; comments→`"[deleted]"`; ratings/votes/hype/reactions KEPT with credibility fields (isEmailVerified/reputation/createdAt) frozen; hard-deletes sessions/tokens/follows/consents/badges; audit trail untouched (denormalized actor label survives). The reputation engine now **skips `deleted` users** so their frozen weight can't drift. **Consent** (`POST /auth/consent`) — append-only versioned records with a **coarsened IP** (same rule as sessions).
- Verified: **`verify:i6` = ALL 51 attack-proof checks PASS** (Slice 7 added 46–48; old 46/47/48 → 49/50/51), all DB truth: export returns the user's data; delete → wrong-password 403, then PII scrubbed + email/username freed + status deleted + comment "[deleted]" + **rating KEPT with the aggregate still 1** (honest, no crash/fabrication) + sessions/tokens/follows/consents gone + old session 401; the freed email re-registers as a NEW active account (202 + fresh verify token) distinct from the tombstone. Static gate: tsc 0, ESLint clean, Prettier clean, **104 backend + 3 frontend tests**. **No-regression ladder green from empty**: `health` + `verify:i1…b2` + `verify:i6`; migration 0010 applies from empty. No new UI → no screenshot gate.
- Next: **I6 Slice 8 — Public UI**: register/login/verify/reset/account pages, public profile (`/u/[username]`), the User Panel (my ratings/comments/followed, level + progress + badges), header sign-in → profile menu, and the community sections filling the I5 placeholders (game rating input + distribution + comments; topic trust/bias votes + discussion; upcoming hype-vote). On the locked design system; UGC rendered escaped (prove the stored `<script>` renders inert). Then the visual gate (desktop + mobile).
- Notes/decisions:
  - Four decisions (owner-approved): tombstone keeps the row + freezes credibility (aggregates honest, and someone can't undo their own extreme vote by deleting); delete re-confirms the password (anti-hijack friction on the most destructive action); both email AND username freed while the audit label survives; append-only consent history (shows WHEN consent changed, not just the current stance).
  - A hard delete was never an option once the 12 cascades were mapped — the delta inspection prevented a real data-integrity bug.

### [Rebuild Phase 6 — I6 Slice 6: Follow + "Your Feed" (follows table, follow/unfollow API, per-user SSR feed)] — 2026-08-15
- Done: Built **Slice 6 — Follow + "Your Feed"** (the first frontend-touching slice of the I6 rebuild), delta-first (no `follows` table existed; the `/community` scope + CSRF hook, the `requireAuth` guard, the auth-BFF relay pattern, and the article↔game/topic link tables were reused). **`follows` table** (polymorphic game|topic, one row per user+entity; migration `0009_i6_follows`). **Follow API** (`/community/follow/:type/:slug`, GET state / POST / DELETE) — **open to unverified users** (decision 6, so a `requireAuth`-based `follower` helper, NOT `requireVerified`), CSRF + per-user rate-limited, addressed by public **slug** resolved to id server-side. **`/community/feed`** composes the follow feed (followed games/topics + their recent linked articles merged newest-first, leak-proof — only public article fields), served **`private, no-store`**. **Frontend**: a `/api/community/[...path]` **BFF relay** (mirrors the auth BFF, adds DELETE); server-side `getSessionUser` + `getFeed` (forward the session cookie to the backend); the **`/feed` SSR page** (`force-dynamic`, per-user; logged-out → sign-in prompt); a **`FollowButton`** client island (fetches follow state, toggles with a CSRF double-submit); follow buttons wired onto game + topic headers; a "Your Feed" nav link. The cached homepage + public pages are UNCHANGED. Notification delivery stays deferred to I8.
- Verified: **`verify:i6` = ALL 48 attack-proof checks PASS** (Slice 6 added 43–45; old 43/44/45 → 46/47/48), all DB/Redis truth: follow OPEN to unverified (200 + DB row) with CSRF required (no-header → 403); the feed is per-user — lists the followed game + topic with `cache-control: private, no-store`, and a logged-OUT request is **401** (never served anonymously, never an empty feed); one follow row per (user, entity), idempotent, unfollow removes it. Static gate: tsc 0, ESLint clean, Prettier clean, **103 backend + 3 frontend tests**. **Visual gate:** 3 headless screenshots (`_shots/i6s6-*.png`) — logged-out feed (sign-in prompt), game page follow pill (no layout regression), and the logged-in feed (captured via CDP with an injected session): sticky Following sidebar + merged Latest stream with per-item "via" attribution. **No-regression ladder green from empty**: `health` + `verify:i1…b2` + `verify:i6`; migration 0009 applies from empty; `/feed` serves 200.
- Next: **I6 Slice 7 — GDPR**: authenticated data export (profile, ratings, votes, comments, follows, consents as JSON); anonymize-and-tombstone account deletion (hard-delete PII incl. sessions/tokens/follows/consents; detach retained content — comments → "[deleted]", ratings/votes kept but user-detached so aggregates + disconnect math stay honest; audit trail survives via the denormalized actor label); the deleted email freed for re-registration; versioned consent capture.
- Notes/decisions:
  - The caching split is the crux and it holds: the feed is `private, no-store` + `force-dynamic` with a 401 for logged-out (not an empty feed); the homepage/public pages stay exactly as cached; follow is a per-user client island on otherwise-cached game/topic pages — personalization cost the pre-built performance nothing.
  - Three design calls (owner-approved): slug-addressed follows resolved server-side; follow points on game/topic/feed; notification delivery staying in I8. The `/community` BFF relay added here also serves Slice 8's write UI.

### [Rebuild Phase 6 — I6 Slice 5: Reputation + levels + badges (credibility-weighted received-signal engine, background job, leak-proof profile)] — 2026-08-15
- Done: Built **Slice 5 — Reputation + levels + badges**, delta-first (the reputation/levelPoints/levelId columns, the userLevels/badges/userBadges tables, the `computeCredibility` curve, and the BullMQ job pattern all already existed — reused, not rebuilt; **no migration** needed). **Reputation engine** (`reputation/engine.ts`): reputation = credibility-weighted RECEIVED helpful reactions + accepted reports + tenure − removed-content − suspension. The anti-farm core: the helpful term is Σ of each reactor's own 0→1 credibility (a throwaway ring has ~0 credibility → adds ~0), and the pass takes a **reputation SNAPSHOT** so it's order-independent and a ring can't bootstrap itself within one run. **Level engine**: reputation → level via `app_settings.reputation` thresholds; `levelProgress` is a 0→1 fraction that never leaks the absolute thresholds. **Auto-badges** (`verified` + `early-voter`): monotonic + ADD-ONLY (never revoke) so a staff grant/revoke and the editorial badges (top-reviewer/trendsetter/bias-hunter/day-one) are never clobbered. **Background job** (`reputation/jobs.ts` + worker processor + boot enqueue), surfaced in `/health/ready`. **Leak-proof profile**: `/auth/me` now returns `level {name, progress}` + badges and **drops the raw reputation number** that previously leaked (decision 11 — users never see the number, thresholds, or that reputation drives vote weight). Admin surface (recompute / status / staff-only breakdown) gated at admin rank. One schema-free behavior extension: reactions may now target a **comment** (validation only) — that reaction IS the "received helpful-vote".
- Verified: **`verify:i6` = ALL 45 attack-proof checks PASS** (Slice 5 added 38–42; old 38/39/40 → 43/44/45), all DB/Redis truth: **self-farm ring FAILS** (10 throwaway up-votes → target rep 9, newcomer) while the SAME up-votes from aged/reputable reactors → rep 20, contributor (credibility, not volume, moves it); a suspended account is zeroed and removing a user's comment drops their reputation; auto-badges monotonic + idempotent (two recomputes → exactly one row each); leak-proof `/auth/me` (level+progress+badges, `reputation=undefined`, no levelPoints/voteWeight/thresholds). Static gate: tsc 0, ESLint clean, Prettier clean, **102 backend + 3 frontend tests**. **No-regression ladder green from empty**: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2/b1/b2` + `verify:i6`; reputation recompute runs at boot on the fresh stack.
- Next: **I6 Slice 6 — Follow + basic "Your Feed"**: a follows table (games + topics), follow/unfollow actions, and a server-rendered follow-based feed. Per-user → must NOT sit on the anonymous cache (short per-user cache or no-store); logged-out visitors keep the existing curated cached homepage untouched. Notification delivery stays deferred to I8.
- Notes/decisions:
  - The side-by-side checks 38/39 ARE the proof: same helpful-vote count moves reputation 9→20 from credible reactors but leaves it at 9 (newcomer) from a throwaway ring — volume alone moves nothing, only credibility does.
  - Four design calls (owner-approved): the helpful signal = a reaction on your comment (reactions extended to comments); auto-badges add-only (automation never fights a staff grant/revoke); the raw reputation number hidden from `/auth/me` (it was leaking); coefficients + thresholds in `app_settings.reputation`.
  - `voteWeight` remains a dormant column: reputation drives vote weight EMERGENTLY via `computeCredibility` (which reads reputation) — there is no direct level→weight link, and users are never told this exists.

### [Rebuild Phase 6 — I6 Slice 4: Community writes (gated /community scope, uniform credibility weighting, comments + reports/auto-hide, hype)] — 2026-08-15
- Done: Built **Slice 4 — Community writes**, delta-first (the community schema room, the I4b credibility engine, `requireVerified`, and the Redis-counter rate-limit pattern all already existed — reused, not rebuilt; **zero** community write endpoints existed before). New authenticated **`/community` scope** (`community/routes.ts`) reached via the BFF (UI is Slice 8): every mutation is gated in depth — **CSRF** double-submit + **`requireVerified`** (decision 6: browse open, rate/vote/comment require a verified email) + a **per-user Redis rate limit** (`community/rate-limit.ts`, the authenticated counterpart to the anonymous limiter, app_settings-tunable, fail-open). **One-per-user** integrity is the DB unique index (upsert/toggle, never a read-then-write race). Writes: game rating (→ enqueues the I4b `recomputeGameRating` background job), article trust-vote (±1), topic bias-vote (per axis; 0 clears), polymorphic comment (stored RAW, escaped at render — decision 8, no server mangling) + reaction toggle, upcoming **hype** toggle, comment report. **Uniform credibility weighting** (decision 13): trust/bias/hype reuse the SAME `computeCredibility` curve via `community/weighting.ts`, credibility-weighted **on read** (low-volume, always-fresh, no new summary tables — ratings keep their pre-computed burst-aware summary). **Comment reports + auto-hide** (`comment_reports` table, one-per-user; distinct reports crossing `app_settings.community.autoHideReports` (default 5) auto-hide the comment, system-audited) + **moderator remove/restore** (new admin routes, section `comments` gated at moderator rank 30, audited). Net-new **`game_hype_votes`** table. Migration `0008_i6_community` (comment_reports + game_hype_votes; clean, no drift). Public zod schemas (session-scoped — the user comes from the session, weight from the engine, never the body).
- Verified: **`verify:i6` = ALL 40 attack-proof checks PASS** (Slice 4 added 25–37; old 25/26/27 → 38/39/40), all DB/Redis truth: verified-email gate (unverified write 403); CSRF on writes; one-per-user (rating/bias/hype); per-user rate limit → 429 at the app_settings cap; UGC `<script>` stored byte-identical + returned as a JSON string; 3 reports auto-hide → moderator (rank 30) restores; trust weighting (weighted pulled above naive toward the credible voter); **review-bomb via real accounts** — flag raises, **weighted 63 vs naive 15** (proven dominate, ring can't move it), vote count intact; per-vote weight at rest proven ~1.0 / verified-new ~0.45 / **unverified ~0**; counter-case — a proven LOW score MOVES to ~35 and is NOT flagged. Static gate: tsc 0, ESLint clean, Prettier clean, **100 backend + 3 frontend tests**. **No-regression ladder green from empty**: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2/b1/b2` + `verify:i6`; migration 0008 applies from empty.
- Next: **I6 Slice 5 — Reputation + levels + badges**: reputation from received credibility-weighted signals (a self-ring must NOT move it) + tenure, minus removed content/suspensions; the level engine as a background job; users see level name + progress + badges and NEVER the formula, thresholds, or that level affects vote weight. Verify includes a self-farm ring failing to raise reputation.
- Notes/decisions:
  - **Honest disclosure (the best catch):** the first review-bomb pass asserted weighted ≥40 and "failed" at 38 — the number was right, my threshold was wrong (mis-applied the damping formula: `dampMult = 0.3 + 0.7·credibility` → a verified-new voter damps to eff **0.277**, not 0.135). Rather than lower the threshold to pass, root-caused it AND fixed the threat model: a real ring is mostly UNVERIFIED throwaways (weight ~0, injected) plus a few verified through the gated flow — which collapses naive (15) while weighted resists strongly (63). A harder, more honest proof (same instinct as the Slice-2 per-IP catch).
  - Three design calls (owner-approved): read-time weighting for the low-volume signals (no summary tables/jobs); hype as a one-per-user weighted toggle; auto-hide at 5, tunable.
  - Verify checks made RE-RUN-SAFE (seeded games/articles accumulate votes across runs): fresh RUN-scoped games for the review-bomb/counter-case, trust/hype vote cleans, and userA fetched by id (the admin list caps at 200 rows).
  - Scope boundary held: Slice 4 = backend writes + weighting + moderation + authenticated `/community` reads; public-page exposure + the UI filling the I5 placeholders is Slice 8.

### [Rebuild Phase 6 — I6 Slice 3: RBAC + admin hardening (staff-session path, per-section rank gating, owner password)] — 2026-08-14
- Done: Built **Slice 3 — RBAC + admin hardening**, delta-first (the `roles` ladder, `SessionUser.role` returned live by `validateSession`, the `x-admin-token` guard, `actorOf`, and passwordHash redaction all already existed — reused, not rebuilt). **Guard primitives** (`auth/guards.ts`): `requireAuth`/`requireVerified`/`requireStaff`/`requireRole(minRank)`, each resolving the signed session cookie ONCE per request (symbol-cached) so stacking costs one DB hit; ranks come from the `roles` JOIN so a role change lands on the user's next request (no re-login — a reason we chose opaque sessions over JWT). **Dual-path admin auth** (`admin/guard.ts`, replacing the token-only hook): a rank-gated **staff SESSION** (the primary human path) OR the **`x-admin-token` service credential** (RETAINED for automation — the hard constraint keeping verify:i1…b2 green). A present-but-wrong token is a hard 401 (no fall-through). Cookie-authed mutations also require the CSRF double-submit header (`csrfOk` extracted to `session.ts`, shared with the /auth scope); the bearer token needs no CSRF and bypasses per-section gating. **Per-section rank gating** (`admin/rbac.ts`, centralized + `app_settings.rbac`-overridable): default ADMIN (40); **owner-only (50)** = `users`, `roles` (privilege-escalation surfaces — independent call, owner-approved); **moderator (30)** = content moderation (`topics`, `articles`, `relations`, `clustering`, `game-flag-reports`, `_meta`); **`bias` deliberately ADMIN (40)** — bias weights tune the engine behind every public number (system tuning, not day-to-day moderation; owner call). **Audit accountability**: a staff action records the REAL username + user id (`actorOf` reads the resolved staff identity), not "service". **Owner password** (`scripts/set-owner-password.ts` + `npm run set-owner-password`): the only place a password enters the system — Argon2id-hashed, marks verified, revokes existing sessions, NEVER prints the password/hash; interactive hidden prompt or `$OWNER_PASSWORD`; the seed `admin` stays passwordless by design. **De-hardcoded the anonymous rate limit** (a "no hardcoded thresholds" golden-rule fix): `RATE_LIMIT_MAX`/`RATE_LIMIT_WINDOW` env, **production default unchanged (100)**, demo raised to 1000 via compose so the single-host verify suite doesn't self-throttle. No new migration (RBAC is code-only).
- Verified: **`verify:i6` = ALL 27 attack-proof checks PASS** (Slice 3 added checks 20–24; old 20/21/22 → 25/26/27), all DB/Redis truth: no-creds→401 vs signed-in-non-staff→403 (distinguished); moderator (30) reaches a moderation section but 403 on admin+owner sections; admin (40) reaches admin but 403 on owner-only `roles`; owner (50) reaches owner + staff mutation 403-without-CSRF/200-with + **audit names the real staff user**; service token reaches the owner-only section a mere admin cannot (retention). Owner-password tool proven end-to-end on the live stack (null→`$argon2id$`, owner login→owner-rank session→owner-only section 200, wrong password 401; reset then wiped it — `admin` passwordless again). Static gate: tsc 0, ESLint clean, Prettier clean, **92 backend + 3 frontend tests**. **No-regression ladder green from empty**: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2/b1/b2` + `verify:i6`.
- Next: **I6 Slice 4 — Community writes**: ratings / article trust-votes / topic bias-votes / comments / reactions / upcoming hype-vote wired to real accounts — each one-per-user, validated, per-user rate-limited, **verified-email gated** (`requireVerified`), uniform 0→1.0 credibility weighting on every signal (decision 13). Verify re-runs the I4b review-bomb with real accounts (unverified ~0 weight, verified/aged ~1.0, burst flag raises, weighted score resists, a genuine proven-voter low score still moves + isn't flagged).
- Notes/decisions:
  - Two judgment calls surfaced at review; both accepted, with the owner moving `bias` 30→40 (system tuning, not moderation) — applied.
  - The de-hardcoded rate limit surfaced from the RBAC verify: the growing single-host suite was about to self-throttle against the hardcoded 100/min. Fixed the underlying golden-rule violation rather than working around it; prod posture unchanged, real DDoS shield remains Cloudflare.
  - Full field-level RBAC granularity + the unified Control Panel remain **I8** per the SPEC — Slice 3 is section-level gating only.

### [Rebuild Phase 6 — I6 Slice 2: Email flows (outbox seam + hashed single-use tokens + verify/reset + send throttles)] — 2026-08-14
- Done: Built **Slice 2 — Email flows**, delta-first (Slice 1's taken-email path already had the hash-burn + a "Slice 2 emails the notice" TODO, so I wired the real send, not a rebuild). **Transactional-outbox seam** (`email/`, same dormant-until-production pattern as IGDB/RAWG/Steam): **Mock** sender writes `email_outbox` rows (the dev mailbox — zero network, no secret); **Live** sender dormant (throws "not configured") until a provider is wired; `getEmailSender()` switches on `isProduction()`, `describeEmailSender()` surfaced in `/health/ready` (`email:{provider:mock,live:false}`). **Single-use hashed tokens** (`auth/tokens.ts`, `user_tokens` table): only **SHA-256(token) persisted** (reuses `hashToken`), raw token lives only in the emailed link; TTL'd (**verify 24h / reset 1h**, both `app_settings.email` knobs clamped-with-defaults); **one active per (user,purpose)** via delete-then-insert in a txn; **race-safe consume** = conditional `UPDATE…WHERE consumed_at IS NULL AND expires_at>now() RETURNING` (a replay can never double-spend). **verify-email** consumes → flips `is_email_verified` → signs the user in; **reset-password** consumes → new Argon2id hash + `password_algo` → `revokeAllSessions` (every prior session dies) → marks verified. **Enumeration-safety finalized**: register taken-email → generic `202` + Argon2 burn + "account exists" notice **to the real owner only** (never the requester); request-verification / request-password-reset always return an identical generic body regardless of existence — `canSend`/send are short-circuited so a **ghost email produces zero outbox rows** (nothing sent, not just an identical-looking reply). **Send throttles** (`email/throttle.ts`): per-recipient (sha256-keyed) **and** per-client-IP Redis counters, thresholds in `app_settings.email`, fail-open on Redis error, "silently skip send + return the same response" posture. Pure template builders (`email/templates.ts`) normalize the base origin (no `//` double slash) and the account-exists notice **carries no token by construction**. Migration `0007_i6_email` (user_tokens + email_outbox + indexes + cascade FK).
- Verified: **`verify:i6` = ALL 22 attack-proof checks PASS** (Slice 2 added checks 13–19; old 14/15 → 21/22), all reading **DB/Redis truth via docker exec**: `user_tokens` = `sha256(token)` ≠ raw + 23–25h TTL band + unconsumed + tokenless reply; verify single-use (consume 200, replay 400) + `is_email_verified` flipped + signed in; account-exists notice `0→1` in the **owner's** outbox while the requester's reply stays byte-identical/tokenless/cookieless; reset sets a new working password + `oldPw 401`/`newPw 200`/**every session revoked**; reset single-use (replay 400) + enum-safe (ghost → identical 202, **0 outbox rows**); **per-email throttle 5/8** and **per-IP throttle 20/22** (cap read from `app_settings`, not hardcoded); all Slice 1 invariants retained (lockout/CSRF/BFF/spoof-flood/admin-redaction/service-token green). Static gate: tsc/ESLint/Prettier clean, **87 backend + 3 frontend tests** pass. **No-regression ladder green** from empty: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2/b1/b2`; migration 0007 applies from empty. **Owner-approved.**
- Next: **I6 Slice 3 — RBAC + admin hardening**: permission guards (`requireAuth`/`requireVerified`/`requireRole(minRank)`/`requireStaff`), staff-session as the primary human admin path, per-section rank gating (moderator 30 < admin 40 < owner 50), and **retaining `x-admin-token` for automation** as the hard constraint (verify:i1…b2 must stay green). This is where the **owner password** is set.
- Notes/decisions:
  - Two disclosures, both in the **verify script, not the product**: (1) check 14 asserted `is_email_verified::text === 't'` but Postgres casts boolean→text as `'true'`/`'false'` (the `t`/`f` is only psql's column *display* format; check 13 already used `'true'`) — assertion bug, product was correct; (2) the per-IP throttle was previously **asserted but never exercised** — adding a real check surfaced that the run sits just under the global `@fastify/rate-limit` (100/min) ceiling, which could fire first and **mask** the specific control (a check that passes for the wrong reason is worse than none). Fixed by sending the probe's registers **admin-exempt** (the token opts out of the global limiter's allowList but does NOT bypass `canSend`, which is route logic keyed on `req.ip`), isolating exactly the per-IP email throttle without spending check 20's budget.
  - `cleanEmailThrottle()` scopes to `gk:email:send:*` only; still never `flushall` on the shared stack.

### [Rebuild Phase 6 — I6 Slice 1: Auth core (sessions + Argon2id + CSRF + lockouts + BFF)] — 2026-08-13
- Done: Reconstructed the lost SPEC-I6 from the 13 locked decisions (owner-approved), then built **Slice 1 — Auth core**, delta-first, with the original build's two adversarial-review findings shipped **closed from day one**. **Sessions = opaque server-side** (`auth/session.ts`): 256-bit token minted once, only its **SHA-256 hash persisted** (`sessions.token_hash`), sliding 14-day expiry under a hard **90-day absolute cap**, 15-min touch throttle, roles+level joined on validate; instant revoke + `revokeAllSessions(except?)` (the GDPR-cascade + reset-all primitive). **Passwords = Argon2id** via `@node-rs/argon2` (prebuilt Rust, no node-gyp — OWASP m=19456/t=2/p=1), `password_algo` stored for future re-hash; `verifyPassword` passes **no options** (params self-describe in the PHC string) — a HIGH-value fix since the old options-path did **5× the work per attempt**. **CSRF double-submit** (readable `gk_csrf` cookie echoed as `x-csrf-token`; onRequest gate on the `/auth` scope). **Enumeration-safe**: register returns a generic 202 with an Argon2 **burn on the taken-email path** (byte-identical body AND timing), and does **NOT** auto-login; login resolves the user first then dummy-verifies the unknown-user path. **Two adversarial findings, closed + proven:** (HIGH) `TRUST_PROXY` now defaults **false** (unspoofable socket peer; BFF strips `x-forwarded-for`/`x-real-ip`) with a hop-count/CIDR parser; (HIGH) **uid-keyed brute-force lockout** (`uid:<id>` so bob/Bob/bob@x.com share one budget) + per-IP flood lockout, Redis counters, thresholds in `app_settings.auth`. Plus (LOW) **constant-time** admin-token compare (hash-then-`timingSafeEqual`) across admin routes + the rate-limit allowList, admin CRUD/audit **redaction** of `passwordHash`, and a **fail-closed production guard** (`assertProductionSecrets` throws if SESSION_SECRET/ADMIN_API_TOKEN are still demo defaults in production). **BFF** (`apps/frontend/app/api/auth/[...path]`) relays cookies both ways same-origin (browser never hits Fastify direct), allowlists forward headers, rejects `.`/`..` path segments. Migration `0006_i6_auth` (sessions table + `users.password_algo`).
- Verified: **`verify:i6` = ALL 15 attack-proof checks PASS** — reading **DB/Redis truth via docker exec**, not trusting API responses: register generic 202 + no session cookie; enumeration-safe register byte-identical; DB shows password only as `$argon2id$` + `password_algo`; signed HttpOnly SameSite=Lax cookie, clean payload; DB shows session as `sha256(token)` ≠ raw + sliding ≤ absolute 90d; `/auth/me` 200/401; CSRF 403/403; logout + logout-all revoke; **uid-keyed lockout blocks even the correct password (429)**; enumeration-safe login timing symmetric (~54ms/49ms); BFF relay + dot-segment 400/404; **rotating-X-Forwarded-For flood still trips the per-IP lock** (spoof proof); admin redaction `[REDACTED]`; `x-admin-token` still authorizes. Static gate: tsc/ESLint/Prettier clean, **83 backend + 3 frontend tests** pass. **No-regression ladder green** from empty: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2/b1/b2`; migration 0006 applies from empty. **Owner-approved.**
- Next: **I6 Slice 2 — Email flows** (EmailSender seam → `email_outbox`, `user_tokens` hashed/TTL'd/single-use with race-safe consume, verify + reset with reset revoking ALL sessions, "account exists" notice to the real owner, send throttles, migration `0007_i6_email`).
- Notes/decisions:
  - Two mid-slice bugs, both root-caused (not suppressed): (1) verify check-11 timing skew (679ms vs 219ms) was the Argon2 `verify()` options-path doing 5× work — fixed to the PHC-self-describing path (real production win); (2) verify check-12 429 was **test cross-contamination** — the check-11 warmup locked userC's own account — fixed with a throwaway userD + `cleanAuthKeys()`, honest about it not being a product bug.
  - Proofs deliberately read **DB/Redis truth**, never the API's own word — the whole point of a security verify is to not trust the thing under test.
  - `cleanAuthKeys()` scopes to `gk:auth:*` only — never `flushall` on a shared stack (the machine rule).

### [Rebuild Phase 5 — B2: Steam data structure (seam + own-history + sweep + chart)] — 2026-08-12
- Done: Rebuilt B2 per the locked decisions — the A/B rebuild's last phase. **The honest premise, encoded:** Steam's Web API gives CURRENT players / reviews / price / DLC but has NO past-players endpoint — SteamDB built its history by recording the current number for years, so we do the same (accumulate our own going forward, link OUT to SteamDB for the deep past) and the UI copy says so verbatim. **(1) Steam seam** (`data-source/steam/`, same dormant-until-production pattern as IGDB/RAWG): Mock = nothing + zero network; Live (production-gated) = `GetNumberOfCurrentPlayers` + storefront snapshot (price / review-% / DLC app ids), `STEAM_API_KEY` read from env at call time, **status-only errors** (a key-bearing URL can never surface in a log or throw). **(2) History store: REUSED `game_player_counts`** — it already carried (game_id, source, current/peak, captured_at) WITH the composite (gameId, capturedAt) index; zero migration, no duplicate table to orphan it. **(3) Sweep** (`steam/jobs.ts` + worker): append-only rows off the request path, per-game try/catch (**one failing app never aborts the sweep**), storefront refresh scoped to Steam rows only — incl. `kind='steam'` on the sentiment update so Metacritic/OpenCritic rows can never be clobbered (caught during build, not in the spec) — repeatable schedule **armed only in production** (`armSteamSync` no-ops in demo; processor registered but dormant), Redis state surfaced in `/health/ready` (`steam.{provider,live,sync.armed,lastSweepAt…}`). **(4) Chart:** sparkline → dated area chart, ONE viewBox'd SVG at width:100% (no media queries — reflows to any width; CSS-collapse lesson): y-scale (nice-ceil gridlines, compact labels), 4 dated x ticks, per-point dots, highlighted latest, amber-only; stats row = current / **"Peak (recorded)"** / week-change — peak semantics FIXED to max-over-recorded-history (the old "24h peak 58k" would have contradicted the 179.8k spike drawn right below it); honest copy + kept SteamDB link-out. Seed: 26 weekly points (~6 months), launch→decay→content-bump→settle (8 games × 26 = 208 rows); public query window 24→40. **(5) DLC:** already existed (A2) — now fed by the storefront snapshot in production via the sweep. New `verify:b2` (9 checks).
- Verified: **From-empty proof** — full ladder green in order: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2/b1/b2`. `verify:b2` = **ALL 9 PASSED** (seam mock + sweep dormant in readiness; 26 ascending dated points / 175 days; shape asserted numerically — spike ≥3× settle + bump ≥1.3×; chart SSR 52 dots + 6 dated ticks + zero iframes; "Peak (recorded)" + honest labels + SteamDB link; **STEAM_API_KEY absent from served HTML + readiness JSON**; chart CSS amber-only). Static gate: tsc/ESLint/Prettier clean, 77/77 tests. **Visual gate:** 3 screenshots (`_shots/b2-*.png` incl. a chart close-up crop) — desktop + mobile reflow — **owner-approved**.
- Next: **I6 rebuild (accounts & community), slice-by-slice** — the owner sends the resume plan (Slices 1–2 were auth core + email flows with two adversarial security reviews; all lost with the SSD, rebuilt from the locked record).
- Notes/decisions:
  - "Don't show a figure that implies something untrue" drove both unscripted fixes (peak label; kind-scoped sentiment refresh) — same principle as the honest history copy.
  - Sweep cadence 30 min (SteamCharts-style granularity without hammering the API), tunable constant; demo never enqueues at all.
  - The A/B rebuild is COMPLETE: I5c ✓ A1 ✓ A2 ✓ B1 ✓ B2 ✓ — the public site now matches the pre-crash state per the locked specs, verified end-to-end from empty.

### [Rebuild Phase 4 — B1: clickable titles + tag filters (no nested anchors)] — 2026-08-12
- Done: Rebuilt B1 per the locked decisions, delta-first. **Root data gap closed:** the mock feed had ZERO article URLs (every "Read at {source} ↗" dead in demo) — the MockFeedProvider now DERIVES realistic per-source permalinks (`{source.website}/articles/{slugify(title)}`, shared `lib/slug`) rather than hand-editing 320 rows; an explicit dataset URL still wins, live RSS URLs untouched, guid idempotency unchanged; flows through the existing `httpUrl` sanitize into `articles.url` (317/317 after reseed). **Titles navigate:** hero trending rows converted `<button>`→`<a href=/topics/…>` KEEPING hover/focus spotlight selection (the locked I5a UX — point selects, click goes); spotlight title + game chip link; feed-card titles → topics; coverage-row headlines (topic + game "in the news" + source-detail) link OUT to the original (same target as the kept "Read at" link — excerpt+link posture); latest-column rows now real outbound anchors (URL fix). **Tag chips → A1 filter URLs:** game-page + upcoming-card + tile genre/platform chips deep-link `?genre=`/`?platform=`; **mode chips stay plain** (no mode facet — nothing pretends to filter). **Nested-anchor restructures:** TopicCard + GameTile were full-card `<a>` wrappers → now plain containers with cover+title as the item links and chips as SIBLING links (UpcomingCard already had the correct pattern — untouched structurally). CSS: `.gk-title-link` (inherit, hover amber), tile genre links styled like the old dim text, chip-link `text-decoration:none` base, cover anchors `display:block`. New `verify:b1` (10 checks).
- Verified: **From-empty proof** — reseed → **entire suite green in order**: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2/b1`. `verify:b1` = **ALL 10 PASSED**, incl. the **nested-anchor audit: max `<a>` depth = 1 on all 8 public surfaces**; permalinks on every aggregated row; trending links + selection preserved; 36 tile-name + 67 genre-chip links on browse; mode-not-linked assertion; a chip URL FOLLOWED and resolving to the filtered catalog with the chip active; leak-proof + canonicals unchanged. Static gate: tsc/ESLint/Prettier clean, 77/77 tests. **Visual gate:** 4 screenshots (`_shots/b1-*.png`) — home desktop+mobile, topic, browse — pixel-equivalent to the locked look, **owner-approved**.
- Next: **Rebuild Phase 5 — B2 (Steam data structure)**, per the owner's spec — the last A/B phase before the I6 slice-by-slice rebuild.
- Notes/decisions:
  - The trend-row conversion is the one UX-sensitive change: `aria-pressed` → `aria-current`, selection handlers kept on the anchor. On mobile (no hover) the spotlight sits above the list, so tap-to-navigate is the right behavior for the "tappable rows".
  - RelatedGames rows keep genres as plain subtext (they're dim metadata inside an existing row link, not chips — linking them would force another restructure for no affordance gain).
  - Cover anchors carry `tabIndex={-1} aria-hidden` — they duplicate the adjacent title link, so keyboard/AT users get ONE stop per card, not two.

### [Rebuild Phase 3 — A2: game-page enrichment (buy links + SteamDB + videos + DLC)] — 2026-08-12
- Done: Rebuilt A2 per the locked decisions, delta-first: schema was ~90% present (`game_prices` already had `url`/discount, `game_videos` had pin/sort, DLC + admin CRUD complete) — so no blind rebuild. Real delta: **(1) "Where to buy"** — multi-store outbound links (Steam URL built from the public `steam_app_id` read off the row; Epic paths; **GOG only on DRM-free-friendly titles** so the demo reads authentically) + price + **amber** discount badge (fixed a locked-rule violation: the badge was green `#7fd29a` — green/red stay reserved for bias/disconnect) + "prices are the stores', not ours" note; renders only where a link exists (never an empty buy box). **(2) "More stats ↗"** — outbound SteamDB charts link on the player panel, only where a Steam id exists; `steamAppId` exposed on GameDetail (public fact); link out ONLY, none of their data ingested. **(3) Videos** — migration `0005_a2_videos` (+`channel`, +`thumbnail_url`); **thumbnail cards that LINK OUT to YouTube** (play icon, TRAILER/REVIEW/GAMEPLAY tag, channel, honest "no autoplay, no embedded trackers" label; thumbnail null in demo → designed CoverArt placeholder) — **zero iframes** (Core Web Vitals + GDPR pre-consent cookies); display = editor-pinned first → sort → take 3; new **videos seam** (`data-source/videos/`: mock = no candidates/no network; live YouTube Data API adapter dormant, key never logged/output) + audit-logged admin trigger `POST /admin/api/games/:slug/videos/autofill` that **only fills empty slots** (curation always wins; editors curate via the existing `game-videos` resource). **(4) DLC seeds** (slot already existed): Cyberpunk→Phantom Liberty, Elden Ring→Shadow of the Erdtree, Witcher 3→Hearts of Stone + Blood and Wine, each linking out to its Steam page. Seeds: 3 mock videos per rated game (8×3=24), multi-store price rows. New `verify:a2` (12 checks).
- Verified: **From-empty proof** — `demo:reset` → migration 0005 applies → **entire suite green in order**: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1/a2`. `verify:a2` = **ALL 12 PASSED** (multi-store links + discounts; authentic spread — Elden Ring Steam-only; SSR outbound rows + amber badge + SteamDB link + video cards + **0 iframes**; DLC; unenriched game renders NONE of the blocks; canonical + leak-proof + **no API-key material in HTML**; autofill = mock/added 0/curation untouched). Static gate: tsc/ESLint/Prettier clean, **77/77 tests** — incl. the recurring health-test flake **root-caused and fixed**: `buildServer()` cold-start > vitest's 5s default under parallel load on this machine → per-test timeout 20s (construction, not assertions). **Visual gate:** 3 screenshots (`_shots/a2-*.png`) — Cyberpunk desktop+mobile, Witcher 3 desktop (−80% + 2 DLCs) — collapse-free, **owner-approved**.
- Next: **Rebuild Phase 4 — B1 (clickable titles + tag filters)**, per the owner's spec.
- Notes/decisions:
  - Everything legal-by-construction: outbound links/API only (Steam/Epic/GOG pages, YouTube watch URLs, SteamDB) — never scraped content, never embedded players, thumbnails as light `<img>` link-outs.
  - Demo video rows ARE the curated list; production autofill proposes only into empty slots — the auto+override rule holds structurally, not by convention.
  - Store seed helper reads `steam_app_id` off the game row (no id duplication in the seed).

### [Rebuild Phase 2 — A1: discovery + catalog pagination + landscape covers] — 2026-08-11
- Done: Rebuilt A1 per the locked decisions, after a delta inspection (per the rebuild rule) showed: covers mostly landscape already (game 4/3, tiles 16/10, upcoming 16/9, spotlight cinematic) EXCEPT the homepage mid-card/hero-secondary cover (~square) — fixed to **16/10** (172px col desktop / 128px mobile, `aspect-ratio` + centered, collapse-safe comment on the rule); pagination + discovery **missing entirely** — built. **Server-side pagination**: `getCatalogData` gains `page` (malformed→1, out-of-range→clamps to last page, never broken), returns `page/perPage/totalPages` + one page slice — the full set is never shipped; **pageSize is an `app_settings` knob** (`catalog.{pageSize}`, default 36 — admin-configurable, nothing hardcoded). **`/games/browse`** = the exhaustive grid: SSR `<Pagination>` (real `?page=N` anchors preserving filters; filter chips deliberately drop `page` so filtering restarts at 1), "Showing X–Y of N · page P of T" line, facets/sort unchanged; canonical rules: filtered→`/games/browse` (dup-content consolidation), unfiltered `?page=N` self-canonical (the crawl path to every game). **`/games` = discovery** (Steam/IMDb model): Top rated, **Most discussed** (new coverage aggregate — distinct articles + outlets per game via `article_subjects`; unrated-but-covered games shown honestly with "—" scores), Browse-by-genre (chips → filtered browse), Coming-soon row (reuses UpcomingCard), "Browse all N games →" CTA; backed by new **`/public/discovery`** (`getDiscoveryData`, shares `loadCatalogRows` with the catalog — same leak-proof surface). Homepage GenreRail now deep-links `?genre=` (was a bare /games link); sitemap lists both hubs. New `verify:a1` (15 checks); `verify:i5b` catalog checks retargeted to `/games/browse` (the page moved; the checks follow).
- Verified: **From-empty proof** — fresh `demo:reset` → boot → **entire suite green in order**: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b/i5c/a1`. `verify:a1` = **ALL 15 PASSED** (discovery sections + leak-proof; 36/199 over 6 pages; page 2 disjoint from page 1; `page=9999`→6, `page=banana`→1; filter+page compose "47 of 199, 2 pages"; SSR CTA/range/anchors; canonical rules; **page-walk covers the whole catalog** — last page exactly the 19-game remainder; both hubs in sitemap; landscape CSS lock). Static gate: tsc/ESLint/Prettier clean, 77 tests (1 known cold-start flake in parallel runs; 2/2 isolated). **Visual gate:** 7 headless screenshots (`_shots/a1-*.png`) — discovery desktop+mobile, browse p1/p2/filtered/mobile, homepage landscape covers — collapse-free, **owner-approved**.
- Next: **Rebuild Phase 3 — A2 (game-page enrichment)**, per the owner's spec (sent per phase).
- Notes/decisions:
  - **i3 verify-residue found (not a regression):** each `verify:i1` run leaves one `typed-topic-*` CRUD artifact (no AI summary by nature); accumulated runs on a long-lived volume blow i3's ≤2-missing tolerance (104/107). Rule: **run suites from fresh volumes** — from-empty they pass.
  - React SSR inserts `<!-- -->` between text and `{expression}` children — verify scripts must match across comment nodes (bit `verify:a1` twice; product was correct both times).
  - Next.js embeds the RSC flight payload in the HTML, so class-name substring counts read ~2× the visible elements — thresholds in checks are ≥, unaffected.
  - Discovery sections may legitimately overlap each other (a game can be both top-rated and most-discussed) — the de-dup rule stays specific to the homepage Top-rated↔Games-in-focus pair.

### [Rebuild Phase 1 — I5c verify-and-lock] — 2026-08-11
- Done: **Recovery session on the new machine** (original SSD unrecoverable; this tree = the June-25 end-of-I5b snapshot). Established the rebuild trunk: git initialized, I5b baseline committed on `main`, pushed to the new private remote (`simeperisic2-eng/gameskeep`); machine-local `.env` isolates the stack on free host ports (**POSTGRES_PORT=5435, REDIS_PORT=6380** — 5432/5433/6379 are taken by other stacks here); stale auto-restarted I5b containers/volume/images wiped first. Then **Phase 1 (I5c: design finalization + flag bias + trending bar + homepage density + topic page + SEO): resolved as VERIFY-AND-LOCK, not a rebuild** — code inspection showed all 7 locked I5c items already present in the I5b trunk (built during the I5a/I5b design iteration): uniform `#1c1810` base (no gradients/seams, dither 0.35), influence-as-flag-chips + quality-as-bar (independent `#7fd29a`, signal `#ecb45c`, red `#e5594a` reserved for the disconnect only), non-sticky trending strip, briefing/genre/community modules + hero-secondary + Top-rated↔Games-in-focus de-dup, designed monogram covers, full BLUEPRINT-3.3 topic page with the collapse-safe `.gk-story-cover`, full SEO layer. **Zero product code changed.** Added the missing **`verify:i5c`** (`scripts/i5c-check.mjs`, 12 checks) locking those decisions: SSR modules, flag-vs-bar split, rendered de-dup, density, 3.3 completeness, collapse-safe cover nesting, canonical + leak-proof, and a CSS design-lock pass (base token, non-sticky trendbar, exact palette, red-reservation).
- Verified: `verify:i5c` = **ALL 12 PASSED**. No regression: `health` + `verify:i1/i2/i3/i4a/i4b/i5a/i5b` all PASS (i4a flaked once under parallel load — its documented transient recompute variance — clean on isolated re-run). Static gate: `tsc --noEmit` clean, ESLint clean, Prettier clean, **77 tests pass** (74 backend + 3 frontend). **Visual gate (the CSS-collapse lesson):** headless-screenshotted homepage + topic page, desktop AND mobile (`_shots/i5c-*.png`) — uniform charcoal, correct flag palette, trending strip, collapse-free topic layout — **owner-approved**.
- Next: **Rebuild Phase 2 — A1 (discovery + catalog pagination)**, per the owner's precise spec (sent per phase; do not design ahead). Then A2/B1/B2, then I6 slice-by-slice.
- Notes/decisions:
  - **Don't rebuild what exists:** the "lost" I5c turned out to be inside the I5b snapshot; rebuilding would have rewritten working approved code. Rule for the remaining rebuild phases: inspect the tree first, build only the real delta.
  - `verify:i5c`'s de-dup check asserts the **rendered** section slugs (served HTML), not the full API arrays — `controversial` legitimately overlaps `topRated` in its unrendered tail (first version of the check was a false positive; fixed).
  - Two **host artifacts** repaired, no project bugs: (1) the zip/copy flattened the `node_modules/@gameskeep/*` workspace junctions → backend tests couldn't resolve `@gameskeep/shared` on the host — junctions recreated (`mklink /J`); (2) Docker Desktop dropped the frontend `:3000` host port-forward under load — `docker compose restart frontend` restores it; headless Chrome must target `127.0.0.1` (IPv4), not `localhost`.

### [Phase I5b] — 2026-06-25
- Done: Built the **public ratings/IMDb side** on the locked I5a design system — four pages, all SSR, engine-consuming only (pre-computed leak-proof DTOs, nothing heavy on the request path). **Game page** (`/games/[slug]`, built + screenshot-gated FIRST per owner): three SEPARATED rating layers (Our / Media Critics / Community-as-two-never-merged-lines) + the critic↔community **disconnect** (band + Δ + sub-levels + editor context tag) + **Content Flags** (shown only where data exists) + our review + player activity (Steam sample-data sparkline) + media (videos/prices/sysreqs/dlc) + in-the-news + related games/topics. After review: cover → flexible **landscape 4/3** slot (covers from any source drop in), added **player-facing data** (player count/peak, Steam %, HLTB, completion, price, sys-reqs — clearly-labeled MOCK), **float magazine layout** (aside floats right, sections reflow full-width once it ends — no dead gap), and a seeded **disconnect context tag** on Stellar Drifter. **Catalog** (`/games`): 199-game grid, genre/platform **facet filters + sort** as pure-SSR anchor chips (crawlable, canonical→/games to dodge filter duplicate-content), separated scores + a disconnect band chip only where the gap is real. **Upcoming** (`/upcoming`, BLUEPRINT 2.4): status + release date + **server-computed countdown**, early-access gated to future/undated 1.0s (no years-old EA launches), **hype-vote = labeled I6 placeholder slot**. **Sources** (`/sources` + `/sources/[slug]`, BLUEPRINT 2.5): per-outlet **ownership + shared-ownership conflict indicator** (parent-company map; siblings list), **reputation** (avg measured coverage quality), commercial-signal share, and a per-source **coverage profile** (reuses the I5a bias-bar + flag model) + recent coverage (excerpt+link only). Backend: `getCatalogData`/`getUpcomingData`/`getSourcesData`/`getSourceDetail` + `/public/{catalog,upcoming,sources,source/:slug}` (source stats computed live from the article feed, editor stat-column overrides honored). SEO: per-page canonical/OG/Twitter/desc, **ItemList(VideoGame)** on catalog+upcoming, **Organization** (with parentOrganization) on source pages, BreadcrumbList everywhere; sitemap now lists the /games·/upcoming·/sources hubs + every game + every source.
- Verified: **From-empty proof** — `demo:reset` (volumes wiped) → fresh boot re-seeds + re-clusters + re-rates → **full suite green**: `npm run verify:i5b` = **ALL 18 I5b CHECKS PASSED** on live served output (catalog API + genre filter subset/echo/match; upcoming slate status+dates soonest-first; sources stats + shared-ownership map; source-detail profile + 404 + leak-proof; catalog/upcoming/sources/source SSR render tiles/cards/countdowns/ownership in HTML with canonical + **0 internal_assessment**; ItemList + Organization + BreadcrumbList JSON-LD well-formed; OG/Twitter/desc; sitemap hubs+source URL). **No regression**: `health` + `verify:i1/i2/i3/i4a/i4b/i5a` all PASS from the same fresh boot. **Visual review (the I5a lesson — verifier reads HTML, not rendering):** headless-screenshotted every page desktop **and** mobile — catalog, RPG-filtered catalog, upcoming, sources index, source detail (IGN + Eurogamer) — all render correctly, no CSS collapse, float layout reflows full-width with no dead gap. `tsc --noEmit` (backend+frontend+shared) clean, ESLint clean, Prettier clean.
- Next: **I6 — accounts/community**: lights up the community rating line, the hype-vote slot, comments/reviews (the slots these pages already render). Or I7 (awards) — the nav link is still a ComingSoon placeholder.
- Notes/decisions:
  - **Owner-gated build order honored:** game page built + screenshot-reviewed FIRST; the three template changes + context-tag seed landed before catalog/upcoming/sources inherited the template; then all three built, each headless-screenshot-checked before sign-off.
  - **Source stats are computed live** from the article feed (article count, affiliate/commercial share from the factual flags, reputation = avg effective quality) — same cheap-aggregate pattern as I5a's homepage genres/source-counts; the schema's `stat_*` columns are honored as editor/job **overrides** when set (auto + manual override rule). No new seed data needed; reflects the actual ingested feed.
  - **Conflict indicator = shared ownership** (parent company owning ≥2 indexed outlets) + commercial-signal share — both drawn from real seeded `parentCompany` + detected affiliate/sponsored flags. Conflict chips use **amber "attention"**, never green/red (those stay reserved for bias/disconnect).
  - **Upcoming "not yet out" rule:** announced/in-development included by status; early-access included only when its 1.0 date is still ahead (or undated) — fixed an early version that listed years-old EA indies (Valheim 2021, etc.) with an "Out now" countdown.
  - **Catalog filters are anchor-link SSR** (no client JS): each chip preserves the other active filters, toggles off when re-clicked, and canonicalizes to `/games` so filter combinations never create duplicate-content. 199 tiles render server-side; filters narrow.
  - **`verify:i3` flake noted:** on one run it read 104/107 topics-with-summary (tolerance allows ≤2 missing) — transient AI-summarization variance in the clustering pipeline (untouched by I5b); passed cleanly on the from-empty re-boot.

### [Phase I5a] — 2026-06-23
- Done: Built the **public frontend foundation + homepage + topic/story page + full SEO pass** on the I4a/I4b data. **Design system** (`apps/frontend/app/(site)/site.css`): ONE uniform warm-charcoal base (`#1c1810`), amber as ACCENT only, calm raised cards, very-subtle pixel-dither, next/font self-hosted (Space Grotesk / IBM Plex Sans / IBM Plex Mono), 8px grid + strict type scale. **Leak-proof public API** (`apps/backend/src/public/`): `getHomepageData` (hero/feed/latest/top-rated/games-in-focus/briefing/genres) + `getTopicDetail(slug)` + `getSitemapTopics`, registered at `/public/*` (anonymous, cached) — explicit column allowlists, `internal_assessment` never selected. **Homepage**: server-rotated hero (trending list + spotlight, no auto-timer), trending chip bar, main feed (load-more, all SSR), latest column, games-in-focus (disconnect), genres, briefing, community teaser, newsletter, labeled AD. **Bias display = two natures** (owner-directed): **influence → factual FLAG CHIPS** (Sponsored/Affiliate/Review copy/Opinion, else Independent) at article level + a **flag distribution** ("9 independent · 1 sponsored") at topic level — never a fake "% influenced" bar, never a verdict; **quality → kept as a scale** (Low-effort/AI ↔ Quality). Influence flags are **neutral/amber**; **red is reserved exclusively for the disconnect** (quality-slop softened red→amber). **Topic page** (`/topics/[slug]`, BLUEPRINT 3.3): header (status/type/linked-game tags/dates), labeled AI summary, bias display (flags + quality + hover "why"), **every source's article row** (round attribution monogram — never the outlet's scraped image; excerpt + link only; per-article flags; author only on `origin='ours'`), **sort** (date/source/influence) + **filter** (independent/sponsored/top-quality) all over SSR'd rows, **developing-story timeline**, related stories, ratings cross-link, community slot (I6), promoted-article AD row. **SEO**: per-page canonical/OG/Twitter/description, **NewsArticle + BreadcrumbList + (where a linked game is rated) VideoGame/AggregateRating** JSON-LD, auto `sitemap.xml` + `robots.txt`, visible breadcrumbs.
- Verified: `npm run verify:i5a` = **ALL 16 I5a CHECKS PASSED** on the live served output — SSR (homepage 8 spotlight panels + 68 flag chips; topic page title + per-source rows + bias flags + AI summary all in HTML), **leak-proof** (`internal_assessment` = 0 in topic HTML *and* JSON, re-proven AFTER i4a planted a real internal assessment), unknown-slug 404, canonical self-points, OG/Twitter/desc present, **JSON-LD well-formed with required fields** (NewsArticle headline/datePublished/publisher, BreadcrumbList trail, AggregateRating 9.4/10 from 13 on a rated-game story), sitemap lists homepage + 105 topics, robots disallows /admin + points at sitemap. **No regression**: `verify:i1/i2/i3/i4a/i4b` all PASS, `npm run health` PASS, **77 tests** (74 backend + 3 frontend), ESLint clean, `tsc --noEmit` (backend+frontend+shared) clean, Prettier clean.
- Next: **I5b — game detail + catalog pages** (`/games`, `/games/[slug]`): the ratings/IMDb face (three layers + disconnect + content flags + videos/prices/sysreqs "where data exists"), to which the topic page's linked-game tags + ratings cross-link already point. Then I6 (accounts) lights up the community slot.
- Notes/decisions:
  - **Bias display reads facts, not a verdict** (owner directive): influence is mostly binary facts, so the public UI names the actual signals as chips (more truthful + transparent + legally safer than "62% biased") — the bias ENGINE (signals/weights/stored breakdown) is unchanged; only the public render changed.
  - **Flags derived in the composition layer** from the already-stored per-article `influenceBreakdown` (signals `sponsored`/`affiliate`/`reviewCopy`/`opinionFraming` → the four flags); read straight from the breakdown so an editor SCORE override never changes the FACTS.
  - **Copyright posture held throughout**: never the outlet's scraped article image (round name-monogram instead), excerpt + link only, article rows link OUT (`rel="noopener noreferrer nofollow"`); game covers use the licensed-cover slot (designed typographic placeholder in demo).
  - **Anti-bug guard found via verify:** a leftover **zero-article topic** (admin/test artifact that still gets a bias distribution from the recompute) was surfacing on the homepage + would render an empty story — now excluded from every public surface (cards, detail → 404, sitemap, related).
  - **Schema/OG image is a placeholder** (`logo.svg`) so JSON-LD validates; a real 1200×630 `og-default.png` is a new **OWNER-TODO**. Quality-slop colour softened to amber so red is the disconnect's alone. Light/dark toggle still deferred to I9.
  - **Layout bug caught by VISUAL review (not the HTML/SSR checks):** the story page rendered scrambled (cover gradient + "The Witcher IV" label painted over the whole page, content obscured) because `.gk-story-cover` lacked `position: relative` + a height, so the `position:absolute; inset:0` `.gk-cover` escaped and filled the positioned `.gk-story`. Fixed by sizing/containing `.gk-story-cover` (mirrors `.gk-spot-cover`). Lesson: the SSR/content/leak-proof verifier reads the HTML correctly but can't see CSS rendering — diagnosed via headless-Edge `getBoundingClientRect` + screenshots (`getBoundingClientRect` is reliable; headless `--virtual-time-budget`/compositor frames can be stale).

### [Phase I4b] — 2026-06-16
- Done: Built the **rating engine** (ratings/IMDb side) on top of the I1 rating tables — independent of the I4a bias/article side. **Three separated layers** (0–100 internal): **Our** (from `game_reviews.ourScore`), **Media Critics** (mean of outlet 0–100 scores, with optional native-scale normalization — "8/10"→80), **Community** kept as TWO never-merged lines (Our-community weighted + Across-the-web Steam estimate). **Critic↔community disconnect** (arithmetic gap + band + sub-levels) with an **editor-only context tag** (judgmental "why" — never auto-inferred, same rule as I4a's "agenda"). **Community weighting + anti-manipulation** implementing the DECIDED BLUEPRINT 2.6 policy. **Content Flags** logic (AI-asset/launch-state/monetization/complexity/DLC) with the "render only where data exists" rule in the DTO. New `apps/backend/src/ratings/`: `rating.ts` (pure — credibility, burst detection, weighted aggregate, disconnect, normalization), `rating-settings.ts` (`ratings` key in `app_settings`), `rating-engine.ts` (DB recompute all/one + audited editor setters), `rating-queries.ts` (admin DTO + per-vote weighting breakdown + the no-data distinction), `jobs.ts` (dedicated `ratings` BullMQ queue/worker). New **migration 0004** (additive): `game_user_ratings`(`ratedAt`, `hasVerifiedPlaytime`); `game_rating_summaries`(naive score, burst flag + info jsonb, critics/community/burst-flag overrides + reasons, disconnect band + detail); `game_critic_reviews`(`nativeScore`/`nativeScaleMax`); `game_content_flags`(`complexityRating`); new `game_dlc` + `game_flag_reports` (community-report slot, structure-only). Admin: `/ratings/{status,settings,recompute,games,game/:id,game/:id/votes,game/:id/{override,burst-flag,disconnect-tag}}` + a functional **`/admin/ratings`** page (three layers, disconnect, per-vote weighting breakdown, override/flag/tag controls, params tuner). Rating data seeded for 3 games (BG3 agree, Cyberpunk mild gap, **Stellar Drifter** large disconnect) + 8 proven demo voters. Recompute runs on boot + admin trigger, off the request path.
- **Community-weighting model (the battle) — two INDEPENDENT mechanisms:**
  - **Credibility (per-vote, 0→1.0, never above) = the PRIMARY defense.** `clamp(email·0.45 + activity·min(1,rep/50)·0.35 + age·min(1,ageDays/30)·0.20 + playtime·0.0)`. Proven voter → ~1.0; unproven (unverified, 0 rep) → 0. Playtime is a STRUCTURE-ONLY slot (0 in demo, Steam later). Stored per-vote in `game_user_ratings.weight`; term-by-term breakdown inspectable.
  - **Burst+extreme detection = the FLAG (+ secondary, credibility-aware damping).** `FLAG ⇔ isBurst AND extremeFraction≥0.60`, two independent conditions. Defaults: windowHours 48, minBurstVotes 15, burstMultiplier 5×, extremeLow/High 15/85, **extremeFraction 0.60** (⚠ flagged in code + below as the FIRST knob to tighten — a sophisticated bomber mixing in 3s/4s could dodge 0.60), dampingFactor 0.30. Damping is credibility-gated: `dampMult = dampingFactor + (1−dampingFactor)·credibility`, so a proven voter caught in a flagged burst stays ~undamped (legitimate anger never muted). Anomaly handling is a VISIBLE flag (`communityBurstFlag` + `communityBurstInfo`), never silent suppression. Critics are NEVER touched by weighting.
  - **Disconnect bands:** agree ≤10 · mild ≤25 · notable ≤40 · large >40; context tag surfaced at ≥26.
  - **The four owner notes, all handled:** (1) extremeFraction 0.60 flagged as first-to-tighten; (2) the counter-case uses **20** proven voters so it CLEARS minBurstVotes (isBurst=true) yet isn't flagged — proving "not flagged" is about non-extremeness, not size; (3) first-ever surge with `historicalRate=0` does NOT auto-flag from lack of history (extremeFraction is the gate); (4) **credibility is the primary defense against a large-volume attack; damping is the secondary, visible layer** (a zero-credibility bomb contributes ~0 regardless of damping).
- Verified: Fresh `demo:reset` → `docker compose up --build` → migrations apply from empty, boot recompute scores the 3 seeded games. `npm run verify:i4b` = **ALL I4b CHECKS PASSED** (headline numbers): **review-bomb** → flag raised, **naive 18 vs weighted 80 (Δ=62)**, all 52 votes counted (40 damped, nothing suppressed), critics 80→80 (untouched), per-vote weights inspectable (proven 0.80 ≫ bomb 0.0); **counter-case** (20 proven rate 30, clustered) → isBurst=true but **NOT flagged** (extremeFrac 0) and the score DOES move (**naive 49 / weighted 49**); **first-surge** (no history) → historicalRate 0, isBurst true, not flagged; **disconnect** → Stellar Drifter gap 44/large, and a freshly-computed gap 46 auto-tag=null until a human sets it; **tunability** (minBurstVotes 15→9999 clears the flag); **override+audit** (community→50, survives a re-tune, auto 80 kept underneath); **content flags** present-vs-absent distinct + report slot; **no-data** distinct from 0 (nulls, no crash). `verify:i1`+`i2`+`i3`+`i4a` = **ALL pass** (no regression). `npm run health` = **ALL CHECKS PASSED**. Hermetic gate: **77 Node tests** (74 backend + 3 frontend; 11 new rating tests incl. the review-bomb scenario), ESLint clean, `tsc --noEmit` (backend+frontend+shared) clean, `next build` clean, Prettier clean. **Python (services/ai) untouched** — gate unchanged from I3. Demo boots one command, zero secrets.
- Next: **I5 — public pages** (homepage hero + bias bar, topic pages, game pages with the three-layer rating block + disconnect + content flags, Upcoming, Sources). SSR for SEO + schema.org (VideoGame/Review/AggregateRating/NewsArticle); the "click to see why" reveals consume I4a's leak-proof bias DTO + breakdown and I4b's `AdminGameRating` DTO (already models the "data exists?" distinction so the clean-page rule is trivial). All engines done + stored — I5 renders pre-computed data, nothing heavy on request.
- Notes/decisions:
  - **Override-safe storage** (I4a pattern): recompute writes only the AUTO columns; `criticsOverride`/`communityOverride`/`burstFlagOverride` (+reasons) + the editor `disconnectContextTag` survive a re-tune. Effective = override ?? auto; disconnect uses effective.
  - **Found & fixed a latent rate-limit bug:** the public limiter (100/min, only `/health` exempt) throttled the **token-guarded admin API**, so bulk staff operations (the I4b verify casts ~176 votes/users) got 429'd mid-run — only 16 of 52 bomb votes landed the first time. Exempted token-authenticated admin requests from the anonymous limiter (`apps/backend/src/plugins/security.ts`); per-account login/abuse limits are I6's job (where rate-limiting actually matters). This also unblocks legitimate bulk imports.
  - **Burst timing uses a new `ratedAt`** (not row `createdAt`) so historical imports + the review-bomb test can simulate a real timeline; production sets actual vote time. Verify-created "proven" accounts have age 0 (age term 0) so they weigh ~0.80 — email+activity carry the credibility in demo; a real aged account reaches 1.0.
  - **Verified-playtime + community-report are structure-only slots** (no logic in demo) — modeled empty/leak-proof for I6/production wiring, mirroring the deferral pattern.

### [Phase I4a] — 2026-06-16
- Done: Built the **bias engine** (news side) + the **clustering secondary gate**, computing on top of I3's captured signals — no re-capture, no re-cluster, no content-NLP. **Two transparent additive axes per article**, stored with a per-score breakdown: **Axis 1 Influence** (0=Independent→100=Influenced, mostly automatic/factual) and **Axis 2 Quality** (0=Slop→100=Top, deliberately humbler/editor-leaning). Each score = `baseline + Σ(named signal weights)`, **clamped 0–100** (the clamp IS the normalization, so every point traces to one named contribution). New modules in `apps/backend/src/articles/`: `bias.ts` (pure compute — `computeInfluence`/`computeQuality`/`aggregateDistribution` + the **allowlist** `toPublicBias`), `bias-settings.ts` (`bias-weights` in `app_settings`, coerce+validate+audit), `bias-engine.ts` (DB recompute of all articles→all topics, source-conflict detection, audited editor **override/note/internal** setters), `bias-queries.ts` (admin + public read surfaces), `event-kind.ts` (mechanical keyword classifier), `gate.ts` (pure `shouldResistMerge`). Wired the gate into `cluster.ts` (between the cosine decision and the write; flips `attach→create` when it should resist) and `pipeline.ts` (computes event-kind/game-ref per article, runs `recomputeBias()` at the end — all background). New **migration 0003**: article `influence/quality_override(+reason)`, `*_breakdown` jsonb, `editor_note`, `event_kind`; topic `bias_distribution` jsonb + `seed_game_ref`/`seed_event_kind` (gate denormalization). Admin: `/bias/{status,weights,recompute,articles,article/:id,article/:id/public,article/:id/override|note|internal,topics}` routes + a functional **`/admin/bias`** page (weights tuner, gate controls, per-article inspect/override/note/internal, article + topic-distribution tables). Gate params + the **event-kind lexicon** both live in `app_settings.clustering` (owner directive: lexicon tunable, not a code change). Bias recompute runs as a background job (`mode:'bias'`), never on the request path.
- **Scoring model + proposed initial weights (owner tunes these like the I3 threshold):**
  - **Influence** = source-commercial-baseline(×0..1, max **15**) + sponsored **+90** (dominant; self-disclosed ⇒ ~99%) + sourceConflict **+40** (parent publishes the covered game) + affiliate **+25** + opinion-framing **+20** + reviewCopy **+15** + paywall **0** (KNOWN-SOFT default — arguably mildly negative since a reader can't verify a paywalled source; left neutral pending real data). Clean independent ≈ baseline only; sponsored+affiliate clamps to ~100.
  - **Quality** = baseline(`35 + reputationGeneral×30`, ⇒ ~50 at rep 0.5; **neutralDefault 50** when reputation unknown) + type review **+10** / opinion **+5** / preview **−5** + sponsored **−15** (PR-rewrite ⇒ Slop) + affiliate **−5**. Narrow spread on purpose (no fake precision); editor override expected to lead here.
  - **Buckets** (topic distribution split): influence/quality **midpoint 50**.
- **Secondary-gate default rule:** resist a cosine "attach" iff **same primary game AND different known event-kind AND candidate topic ≥ `minEventGapDays` older** (default **2 days** — flagged as the FIRST knob to tune on real clustering; `requireDifferentEventKind=true`, `enabled=true`). Only resists merges, never forces one; `other`/missing event-kind or game never fires (conservative); editor merge/split still overrides. Event-kind = mechanical keyword classifier (delay/release/trailer/leak/sales/update/legal/business/review/other), lexicon admin-tunable.
- **Internal-field wall (structural):** `toPublicBias` is the only sanctioned public producer — it builds the DTO field-by-field from an allowlist (never spreads the row), the public query omits `internal_assessment`, and a unit test feeds a row WITH the field populated and asserts it's absent. So the internal-only assessment is incapable of entering any public payload. The judgmental `editor_note` is the opposite: editor-entered only, public-eligible, never auto-generated.
- Verified: Fresh `demo:reset` → `docker compose up --build` → migrations apply from empty, pipeline scores **209 articles + 90 topic distributions** on boot. `npm run verify:i4a` = **ALL I4a CHECKS PASSED**: (1) fresh-boot scoring; (2) **direction** — influence clean **15** < affiliate **25** < sponsored **100**, quality substantive **60** > PR-rewrite **25**; (3) **explainability** — `Σ=115 → clamp 100` = `sponsored+90 affiliate+25` (no unexplained numbers); (4) **tunability** — sponsored weight 90→40 moved auto 100→65; (5) **factual/judgmental** — zero auto notes, note appears only after a human writes it; (6) **internal separation** — present in admin payload, structurally absent from the public payload (keys listed, no `internalAssessment`); (7) **override+audit** — override→7 (auto 25 kept underneath), audit-logged, and **survived a re-tune** (auto recomputed 25→5, effective stayed 7); (8) **topic aggregation** — counts add up (11 = 10+1 = 5+6); (9) **gate** — the same-game/same-register Cyberpunk pair (prod vs sales) stays **2 separate topics** while GTA 6 delay still merges across **6 outlets into one**, and with the gate **OFF** the pair over-merges into one (proving the gate is what separates them) without breaking the legit merge. `verify:i1` + `verify:i2` + `verify:i3` = **ALL pass** (no regression; i3 now 208 articles → 90 topics incl. the 5 new verify-critical articles). `npm run health` = **ALL CHECKS PASSED**. Hermetic gate: **66 Node tests** (63 backend + 3 frontend) pass (15 new bias/gate tests), ESLint clean, `tsc --noEmit` (backend+frontend+shared) clean, `next build` clean, Prettier clean. **Python (services/ai) untouched** — its gate is unchanged from I3. Demo boots one command, zero secrets.
- Next: **I4b — rating engine** (the ratings/IMDb side): three separated rating layers (Our / Media Critics / Community + Steam estimate), 0–100 internal / 1–10 display, **critic↔community disconnect** calc + context tag, **community weighting** (0→1.0) + anomaly damping, game-page **Content Flags** logic. Works on the Game + rating tables from I1, independent of the bias/article side built here. Then **I5** renders the public bias bar from the `bias_distribution` + per-article breakdown stored now (consume `toPublicBias` — it cannot leak the internal field).
- Notes/decisions:
  - **Clamp-as-normalization (not weighted-average/logistic)** so the breakdown stays literally additive — the SPEC's core demand. A learned/opaque model was explicitly out of scope.
  - **Override-safe storage:** auto score + breakdown live separately from `*_override(+reason)`; effective = `override ?? auto`. Re-tuning recomputes only the auto side, so a human decision is never clobbered (same rule as I2 upserts). Topic distributions use effective scores.
  - **Source-conflict (COI) signal** is implemented but mostly **dormant in demo** (the 10 outlets' parents don't publish the catalog games). Correct logic; fires where ownership/publisher data exists (OWNER-TODO).
  - **Gate verify needed a seed:** the I3 "PAIR 4" over-merge wasn't in the committed feed, so I added a verify-critical same-game/same-register Cyberpunk pair (Project Orion **production** vs **30M sales**, ~3 days apart, shared celebratory register → high cosine) + 3 bias direction-spanning articles (clean review / affiliate guide / sponsored PR-preview), all with stable guids.
  - **`recomputeBias` recomputes all articles each ingest** (~210 rows, background) — simple + always-consistent; fine at this scale. A weight re-tune enqueues a `mode:'bias'` job (no re-ingest).

### [Phase I3] — 2026-06-15
- Done: Built the **news engine** — article pipeline + **real clustering**. **Article-source seam** (`apps/backend/src/data-source/articles/`): `ArticleSourceProvider` interface + one `RawFeedItem` shape; **MockFeedProvider** (bundled **~203-item** feed across the **10 sources** from BLUEPRINT 2.5, clustered around ~40 real-world-style events + standalones, zero network) as the demo default; **LiveFeedProvider** (per-source **RSS-first** adapters via `fast-xml-parser`, throttled + retry, excerpt-only + attribution) fully wired but **dormant** (guarded to throw in demo, never fetches); `getArticleSourceProvider()` is the one switch + `describeArticleSource()`. The canonical 10 sources live in `sources.ts` (shared by the live adapters + an idempotent `ensureSources()`). **Embedding engine is REAL** (`services/ai`): added `/embed` + `/summarize` to the FastAPI service using **fastembed** (`all-MiniLM-L6-v2`, ONNX Runtime, **no torch**, **384-dim** = the I1 pgvector columns). Model is **baked into the image at build time** (`python -m app.embedding`) into `/models`; runtime loads `local_files_only=True` → **zero network on boot** (proven via `docker run --network none`). Summaries are neutral **extractive** (deterministic, offline). **Clustering engine** (`apps/backend/src/articles/`): defensive `sanitizeFeedItem`, factual `detectSignals` (affiliate/sponsored/review-copy/paywall/type — raw capture only, scoring is I4), `embedding.ts` (pgvector helpers + pure `decideCluster`), `cluster.ts` (per-article cosine-similarity search over **open** topics within a **time window** → attach-if-≥-threshold-else-create, centroid maintenance, primary topic, timeline, status auto-maint), `settings.ts` (admin-tunable threshold/window in a new **`app_settings`** table, audit-logged), `pipeline.ts` (pull→embed→cluster→attach-games→summarize, idempotent by feed guid, `reset` re-clusters), `merge-split.ts` (editor **merge/split/reassign**, all audit-logged), BullMQ `articles` queue/worker + Redis state. Articles attach to games via I2's **`resolveOrQueue`** (auto-create or unmatched queue). New **`articles.external_guid`** (unique idempotency key) + `app_settings` table (migration `0002`). Admin: `/clustering/{status,topics,settings,ingest,merge,split,reassign}` routes + frontend **`/admin/clustering`** page (tune, re-cluster, merge/split/reassign, topic+source overview). Readiness surfaces `articleSource` + last ingest. Compose: gave the **worker** `AI_SERVICE_URL` + an `ai` dependency (it now calls the AI service for embeddings — the I3 analog of the I2 worker-DB fix).
- Verified: Fresh `demo:reset` → `docker compose up --build` → migrations apply from empty, then the **background pipeline turns the mock feed into 203 articles → 86 topics**. `npm run verify:i3` = **ALL I3 CHECKS PASSED**: seam=Mock (no network); multi-source **GTA 6 delay = ONE topic across 6 outlets**; the **three distinct GTA 6 events (delay/trailer/leak) = THREE separate topics** (the owner's exact fear, solved); 203/203 embedded + primary topic each; topics carry a generated TL;DR + AI summary (85/86); 186 articles attached to catalog games; the unknown game ("Chronowraith Saga IX") filed to the unmatched queue; **idempotent re-run (203→203, 86→86)**; **threshold measurably changes clustering: 0.9→184 topics, 0.5→86, 0.2→4**; time window keeps the ~17-month-old article in its own topic; merge/split/reassign work and are audit-logged. **Per-event purity (independent probe on the feed):** of the **35 seeded multi-article events, 35/35 collapsed to exactly ONE topic — 0 splintered**, and **0 cross-event over-merges** (no topic held articles from two distinct seeded events). So the 87-topic count is not under-clustering: it's 35 event-topics + the standalone single-article items each correctly forming their own topic. (The PAIR 4 over-merge is a deliberately harder same-game/same-register pair than anything in the feed; on the feed itself, same-game events like the GTA 6 trio separate cleanly at 0.50.) `npm run verify:i1` = **ALL pass**, `npm run verify:i2` = **ALL pass** (no regression). `npm run health` = **ALL CHECKS PASSED**. Hermetic gate: **48 Node tests** (45 backend + 3 frontend) pass, ESLint clean, `tsc --noEmit` (backend+frontend+shared) clean, `next build` clean, Prettier clean. **Python gate: ruff clean, mypy strict clean, 12 pytest pass** (embedding tolerance + semantic adversarial + summarize). Offline proven: AI image embeds with `--network none` (dim 384, norm 1.0). Demo boots one command, zero secrets.
- Next: **I4 — bias + rating system.** Compute the two public bias axes (Influenced↔Independent, Slop↔Top) from the raw signals captured here + source reputation; the three rating layers (0–100 internal), disconnect calc + context tag, community weighting (0→1.0) + anomaly damping; game-page Content Flags. The captured signals (`hasAffiliateLinks`/`isSponsored`/`basedOnReviewCopy`/`isPaywalled`/`articleType`) + per-article/topic storage are structured so I4 computes on top without churn. **Also (carried from the I3 adversarial finding): add a clustering secondary gate** using the per-article signals + game links already captured — e.g. *same primary-game + a time gap + different event type → resist auto-merge* — to fix the same-game over-merge (the 0.68 Cyberpunk case) that a single cosine threshold can't. Architecturally enabled now; out of I3 scope.
- Notes/decisions:
  - **Embedding = fastembed all-MiniLM-L6-v2 (ONNX, no torch), demo engine == production engine** (owner-directed). Chosen over a lexical TF embedding because the owner's fear is *same-game-different-event* splintering: measured cosine gaps are decisive — same-event paraphrases ~0.6–0.85, **same-game-different-event ~0.35**, different-game ~0.17–0.25. **Default threshold 0.50** sits squarely in that gap; **time window 14 days**. Both admin-tunable in `app_settings`.
  - **Zero-network-on-boot is proven, not asserted:** model fetched at `docker build`, loaded `local_files_only=True`; `docker run --network none … embed()` returns a 384-vector. Image ~696MB (acceptable for the real engine). `libgomp1` added to the AI image for ONNX Runtime.
  - **Determinism test is tolerance-based** (not bitwise): dim=384, norm≈1, self-similarity≈1 within 1e-4, plus a **semantic adversarial test** (same-event > different-event; the GTA6 trio is separable) — the threshold is tuned against these, not the reverse.
  - **Idempotency** keys on `articles.external_guid` (the feed guid); re-runs dedupe and never splinter. `reset` wipes only feed-produced data (aggregated articles + embedded topics) and re-clusters — used for threshold tuning and the verify's measurable-threshold proof.
  - **Real engine, mock input:** going live swaps only `APP_MODE` (feed provider mock→live RSS); clustering/embedding/summary code is unchanged. The 10 RSS URLs + robots/ToS/legal review are an OWNER-TODO before enabling live pulls.
  - **Known limit (independent adversarial test, accepted for I3):** single-vector + one-threshold clustering **over-merges same-game / same-register events** — e.g. a Cyberpunk 2077 "sequel enters pre-production" vs "hits 30M sales" pair scored **0.68** where they should separate, while a legitimately-same Helldivers launch-server pair scored **0.50**. Because the should-separate pair scores *higher* than a should-merge pair, **no global threshold can satisfy both** — this is the structural ceiling of the approach, not a tuning miss. Default stays **0.50** (raising it would break the 0.50 true-merge). The I3 remedy is **editor split** (mandated, working). **I4 will add a secondary same-game gate** (see below) to reduce these automatically.

### [Phase I2] — 2026-06-14
- Done: Built the **game-data layer + broad seed**. **Data-source seam** (`apps/backend/src/data-source/games/`): a `GameDataProvider` interface + ONE normalized Game shape; **MockProvider** (bundled **~196-game** dataset, real-world-style, **zero network**) as the demo default; **LiveProvider** (IGDB primary → RAWG fallback, Twitch-OAuth token cache, ~4 req/s throttle + 429/5xx backoff, gentle paginated seed pull) fully wired but **dormant** (throws a clear keys-missing error if reached); `getGameDataProvider()` is the **one switch** (demo→Mock / production→Live) + `describeGameProvider()`. **Catalog engine** (`apps/backend/src/catalog/`): defensive `sanitizeNormalizedGame` (clamps/dedupes/drops bad URLs, never trusts external shape), idempotent slug-keyed `upsertGameFromNormalized` (no clobber of editor edits), `importCatalog` (skip-if-populated), `resolveGameByName` (DB → provider auto-create) + `resolveOrQueue` (files unmatched), plus retry/link/create/dismiss editor actions — **all audit-logged**. **Background `catalog-import` BullMQ job** + worker processor (off the request path; the exact pattern I3's article pulls reuse); API enqueues it on boot (demo) with retry/backoff. New **`unmatched_games`** table + `unmatched_status` enum + **`games.external_refs`** jsonb (migration `0001`). Admin: `unmatched-games` registered as a generic resource **plus** catalog routes (`/game-resolve`, `/unmatched-games/:id/{resolve-link,resolve-create,retry,dismiss}`, `/catalog/{import,status,upcoming}`); frontend **`/admin/unmatched`** page (resolve workflow + a name-resolver tester). Readiness now surfaces `gameProvider` + last catalog import. Shared HTTP helpers extracted to `admin/http.ts`; `slugify` centralized in `lib/slug.ts`.
- Verified: Fresh `demo:reset` → `docker compose up --build` → migrations apply from empty, inline I1 seed loads, then the **background import loads 190 games** (188 imported + 2 deduped with the inline seed; 0 invalid), **idempotently** (manual re-import 190 → 190). Statuses spread across all five (announced/in_development/early_access/released/delisted); **8 upcoming** queryable. `npm run verify:i2` = **ALL I2 CHECKS PASSED** (seam = Mock in demo / Live in prod, no live call; broad catalog; idempotent re-import; auto-resolve match + **provider auto-create** of a `seed:false` title; unknown ref → unmatched queue → editor link/retry/dismiss; audit-logged; Upcoming queryable). `npm run verify:i1` = **ALL 23 still pass** (no regression). `npm run health` = **ALL CHECKS PASSED**. Hermetic gate: **31 tests pass** (28 backend + 3 frontend, vitest), ESLint clean, `tsc --noEmit` (backend+frontend+shared) clean, `next build` clean, Prettier clean. **Python (services/ai) untouched.** Demo boots one command, zero real secrets.
- Next: **I3 — article pipeline + clustering.** Mock feed → per-source RSS adapters → normalize → **real clustering engine** (embeddings via the AI service, similarity threshold, time window) → Topics, with merge/split + threshold tuning in admin. I3 calls **I2's `resolveOrQueue`** to attach articles to games (unknown games auto-create or file to the unmatched queue).
- Notes/decisions:
  - **Catalog load is a background job, not inline** (owner-approved) — honors "heavy work off the request path" and is exactly what I3 reuses. The tiny inline I1 seed stays so I1 invariants hold at `ready`; `verify:i2` polls for the catalog to appear.
  - **Found & fixed a latent compose bug:** the `worker` service had **no `DATABASE_URL`** (it only ever used Redis for the heartbeat before I2), so the catalog import failed to reach Postgres. Added `DATABASE_URL` + a `postgres` dependency to the worker, plus job retries/backoff so a transient boot hiccup self-heals.
  - **Idempotency** is keyed on the deterministic slug (shared `slugify`); upserts never overwrite an existing game (auto + manual override). cyberpunk-2077 / baldurs-gate-3 use the same slugs as the inline seed so they dedupe.
  - **`seed: false` entries**: resolvable via the provider but NOT bulk-loaded, so the auto-resolve → auto-create path is demonstrable distinct from a plain DB hit.
  - **Provider id mapping** lives in `games.external_refs` (jsonb) so the live IGDB/RAWG path can round-trip / de-dupe by external id with no further migration.

### [Phase I1] — 2026-06-14
- Done: Built the full **data layer**. **38 tables** across the six core models + generic **Subject** (Drizzle `schema/` split by domain): Subject, Topic (+ M2M subjects, timeline, related), Article (+ M2M topics w/ one-primary, M2M subjects), Game as a Subject specialization (+ review, critic/user/external ratings, rating-summary, content-flags, videos, prices, sys-reqs, player-counts), Source, User (+ roles/levels/badges), Awards (edition→edition-category→nomination→outcome/vote), community tables (schema-room), and an immutable **audit_logs**. Extensible lists (roles, levels, topic/source types, badges, award categories) are **lookup tables (data)**; fixed domains are pgEnums. **pgvector(384)** columns on Topic+Article (empty, ready for I3). DB-level integrity: one-review-per-game, one-rating-per-user-per-game, partial-unique one-primary-topic, copyright CHECK (`body` only when origin='ours'), self-relation CHECK. New **`packages/shared`** workspace (Zod create/update schemas + domain constants, single source of truth). **Admin API** (`/admin/api`, token-guarded) — generic validated CRUD for ~25 resources via a resource registry, relation link/unlink routes, `_meta` + `_audit`, every write **audit-logged** (old→new). **Generic admin UI** at `/admin` (Next.js, metadata-driven forms + token-injecting proxy) to CRUD everything in the browser. Migrations + idempotent demo seed run on backend boot.
- Verified: Fresh `demo:reset` → `docker compose up --build` → migrations apply from empty DB, seed loads, backend healthy. `npm run health` = **ALL CHECKS PASSED**; `npm run verify:i1` = **ALL 23 I1 CHECKS PASSED** (CRUD each model, M2M Topic↔Subject + Article↔Topic primary + Article↔Subject, one-review/one-rating 409s, copyright reject 400, audit old→new, pgvector columns present, extensible-list-as-data, validation rejects bad input, awards edition→category→nomination, cascade delete). Hermetic gate green: Node tests **15 pass** (12 backend + 3 frontend, vitest), ESLint clean, `tsc --noEmit` (backend+frontend+shared) clean, `next build` clean, Prettier clean. Admin UI SSRs real seeded data. Python (services/ai) untouched — unchanged from I0.
- Next: **I2 — game data + seed.** Fill the Game/Subject model from IGDB (primary) + RAWG (fallback) behind the demo↔production data-source seam (`apps/backend/src/data-source/`), with a local mock seed and an "unmatched game" admin queue. The Game/Subject tables and seam are structured so I2 plugs in without schema churn.
- Notes/decisions:
  - **Subject vs Game:** a Game is a `subjects` row (type='game') + a `games` row (unique `subject_id`). Topics/Articles link to `subjects` (works for studios/publishers later with no refactor). The admin presents Game as one flat object; a composite resource keeps the two rows in sync; deleting a Game deletes its Subject (cascades links).
  - **Extensible lists = lookup tables; fixed domains = pgEnums.** Value arrays for the enums live in `@gameskeep/shared` so DB enums and Zod validators can't drift.
  - **Admin = API + generic UI** (owner will manage data in the browser from here on). Reads hit the backend directly; writes go through a Next proxy that injects `ADMIN_API_TOKEN` server-side (token never reaches the browser). Verified live via `scripts/i1-check.mjs`; pure pieces unit-tested hermetically (`npm test` needs no DB).
  - **Drizzle error wrapping:** Drizzle wraps pg errors in `DrizzleQueryError`; the SQLSTATE code is on `.cause`, so the admin error mapper unwraps it (23505→409, 23514→400, …). Migrations run programmatically on backend startup (single migrator; the worker doesn't migrate). Drizzle-generated files added to `.prettierignore`.
  - **Stack left running** after verification (admin at http://localhost:3000/admin). Stop with `npm run demo:down`; wipe with `npm run demo:reset`.

### [Phase I0] — 2026-06-14
- Done: Stood up the full skeleton as an npm-workspaces monorepo. **Backend API** (Fastify 5 + TS) with Zod-validated env, helmet/CORS/rate-limit, `/health` (liveness) + `/health/ready` (probes Postgres+pgvector, Redis, AI service, heartbeat). **Frontend** (Next.js 16 App Router, SSR) landing page that fetches backend status and shows "GamesKeep — foundation OK" + per-service health; base SEO metadata + security headers. **AI microservice** (FastAPI) with `/health` + `/ping` echo. **Postgres** (pgvector image, extension enabled via init SQL) + **Redis**. **Background jobs** via BullMQ: repeatable "heartbeat" job, processed by a dedicated **worker** container, observable through readiness. Demo↔prod **data-source seam** + caching seam present. One-command boot via **docker compose** (postgres, redis, ai, backend, worker, frontend). `scripts/health-check.mjs` = `npm run health`.
- Verified: Fresh `docker compose up --build` → all services healthy; `npm run health` = **ALL CHECKS PASSED** (backend liveness + foundation-OK, Postgres connected, **pgvector 0.8.2 installed**, Redis connected, AI reachable direct + via backend, heartbeat ran, frontend SSR). Boots with **zero real secrets** (no `.env`). Node tests 5 pass (vitest), Python tests 3 pass (pytest); ESLint + ruff clean; `tsc --noEmit` (backend+frontend) + mypy strict clean; `next build` + Docker image builds succeed; Prettier clean.
- Next: **I1 — data layer.** Add the six core models (Topic, Article, Game, Source, User, Awards + generic Subject) to `apps/backend/src/db/schema.ts`, generate the first Drizzle migration (infra already wired in `drizzle.config.ts`), and build admin CRUD. The empty schema + migration config are the obvious extension point.
- Notes/decisions:
  - **Stack choices:** Fastify (built-in validation = bug-prevention), Next.js (SSR for SEO from day one), FastAPI, Drizzle ORM (first-class pgvector), BullMQ (Redis jobs), Zod (env + future input validation), Vitest/ESLint/Prettier (Node), pytest/ruff/mypy (Python).
  - **Runtime versions** resolved newer than expected: TypeScript 6, ESLint 10, Vitest 4, Next 16, React 19, Fastify 5, Zod 4, BullMQ 5. All green.
  - **BullMQ + ioredis dual-package:** BullMQ pins ioredis exactly (5.10.1) so it can't dedupe with our 5.11.1. Resolved by passing **connection *options*** (not an ioredis instance) to BullMQ, so no instance crosses the package boundary (`apps/backend/src/queue/connection.ts`).
  - **Backend runs via `tsx`** (no build step); "build" = `tsc --noEmit` typecheck. Frontend uses Next **standalone** output for a lean image. Worker is the same backend image with a different command.
  - **Known minor:** `drizzle-kit` and Next pull dev-only transitive advisories (`npm audit`); not in the runtime path — revisit during I9 hardening. Starlette TestClient logs an httpx deprecation warning (tests pass).
  - **Stack left running** after verification (visit http://localhost:3000). Stop with `npm run demo:down`; wipe data with `npm run demo:reset`.
